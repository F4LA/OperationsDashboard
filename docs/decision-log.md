# Decision Log — Operations Dashboard

Append-only. Every time the spec (`spec.md`) is ambiguous or silent and a call is made, it gets a new row here. Never edit or delete a past row — if a decision is reversed, add a new row that supersedes it and mark the old one's Status accordingly. New rows go at the TOP of the table, directly under the header. IDs are sequential.

| ID | Date | Decision | Context | Who | Status |
|---|---|---|---|---|---|
| D-109 | 2026-08-19 | Toda escritura de la app pasa por un OVERLAY MODAL BLOQUEANTE: aparece al iniciar cualquier write, cubre la aplicación entera, impide todo clic y se retira solo al confirmarse el resultado. Vive ENCIMA DE TODA LA APP y se dispara desde el único camino de escritura, no dentro de una vista: un guard por vista reaparece roto en la vista siguiente, que es literalmente cómo nació D-098. Reglas: (a) éxito → se retira solo y queda el toast que ya existe; (b) error → el overlay NO se retira solo, muestra el error y exige un clic de reconocimiento, porque un fallo que desaparece a los tres segundos en una pantalla compartida no lo lee nadie; (c) cubre las tres vistas y las cuatro acciones que hoy quedaban sin guard (agregar a la semana, quitar, deshacer, mover a la semana siguiente), además de las que ya lo tenían de forma dispersa. Se descartó la alternativa de deshabilitar solo el botón clickeado más los demás botones de escritura, que era la propuesta de Claude: Bernardo la rechazó con el argumento correcto — D-101 ya fija que el estado se carga ANTES del L10, así que esperar unos segundos con la pantalla bloqueada no le cuesta nada a nadie, y una sola cosa visible es más clara que una regla sutil sobre qué control quedó vivo. | Reportado por Miguel: aplastó "Add to week", no pasó nada visible y volvió a aplastar varias veces. Trazado en el código: postAndRefresh (todos.js línea 1026) no deshabilita nada ni muestra estado, y el write-then-verify con backoff 0/500/1000/2000 ms más la relectura completa del Sheet corre varios segundos sin una sola señal. Miguel hizo lo razonable. Queda registrado que NO hubo tarea duplicada, contra la hipótesis inicial: los pins pliegan por (Task ID, Action) tomando el último por Timestamp, así que N clics al mismo lunes colapsan en un valor — el daño fueron filas basura en el Events log, no compromisos duplicados. El alta ad-hoc, el dropdown de estado y confirmar semana ya tenían guard propio; el hueco era exactamente el conjunto de botones que pasa por postAndRefresh. | Bernardo | Locked |
| D-108 | 2026-08-19 | QUITAR UNA TAREA DE LA SEMANA deja de depender de si la semana está confirmada y pasa a depender de si la tarea está en la LISTA CONGELADA del confirmWeek. Tarea FUERA del array congelado (agregada después de confirmar) → botón Remove simple, sin motivo, igual que en modo armado. Tarea DENTRO del array congelado → sin Remove; siguen mover, descartar y cancelar con motivo obligatorio (§11.4), sin cambios. CORRIGE EL ALCANCE DE D-095, que ató el Remove al modo armado: su razonamiento —"después de confirmar, sacar algo es un cambio de compromiso y no una corrección"— es correcto para lo que estaba en la lista congelada y FALSO para lo agregado después, que nunca fue compromiso de nadie y que el denominador congelado (D-070) no contiene. Ninguna maquinaria nueva: weekCommitment ya está del lado del cliente (todos.js línea 216) y el Remove es el unpin que ya existe. | Reportado por Bernardo a partir del error de Miguel: agregó tareas por error con la semana YA confirmada y no tenía salida. Las tres salidas existentes son todas equivocadas para un clic errado, y esa es la razón de la fila: cancelar una tarea de Rock la saca del plan del sprint entero (D-068c) y no de la semana de una persona; descartar es solo para ad-hoc; y mover a la semana siguiente la compromete a la semana siguiente y además la cuenta como movida en la tasa del §12, o sea que un clic errado penaliza el número de la persona. | Bernardo | Locked |
| D-107 | 2026-08-19 | SE ELIMINA "Both" DEL ESQUEMA. El campo owner de una tarea de plan acepta un nombre de people[] o un ARRAY EXPLÍCITO de nombres (["Brent","Bernardo"]), con al menos uno, todos ∈ people[] y sin repetidos. El literal "Both" pasa a ser ERROR BLOQUEANTE con código propio en validate.js — nunca se re-expande ni se acepta en silencio, porque el modo de falla fue silencioso y la única defensa real es que la palabra no exista. Se resuelve en un helper compartido; hoy la misma expresión está copiada en engine.js (línea 178), todos.js (línea 258), thisweek.js y validate.js. Las tareas ad-hoc no cambian: exactamente un dueño, "Both" sigue rechazado por el servidor (D-066c). Alcance de datos: las 18 tareas de R3 con owner "Both" pasan a ["Brent","Bernardo"] en sprint-plan.json y en data/rock3-seed.json, más la de tests/scenario-live-mode.json. CAUSA RAÍZ: "Both" nunca significó "los dos dueños del Rock" sino "todas las personas de people[]" (people.slice()). Con people ["Brent","Bernardo"] las dos lecturas coincidían y el defecto era invisible. D-106(a) agregó "Miguel" a people[] — obligatorio, sin eso R1 no validaba — y en ese instante las 18 tareas de R3 pasaron a ser también de Miguel, sin un solo error. DAÑO MEDIDO, no estimado: corrido el engine.js real del repo contra el sprint-plan.json commiteado, las 21 tareas de R1 quedaron desplazadas y el último finish de R1 pasó de 2026-08-26 a 2026-09-23, porque el §4.3 hace que una tarea Both ocupe la cola de TODOS sus dueños y las 18 de R3 se serializan contra Miguel. Es una SEGUNDA causa del DEADLINE MISSED de R1-M11 que D-106 atribuyó solo a las 15 tareas sin marcar: marcar no lo arregla. Se construye ANTES de cargar R2 — con Emery en people[] "Both" pasaría a significar cuatro personas y el corrimiento crece otra vez. Se descartó asignar cada tarea Both a un solo dueño: son tareas conjuntas reales y el motor debe esperar al más tarde. | Reportado por Miguel al abrir su vista de To-dos y ver tareas de R3 que no son suyas, incluidas cuatro en "Done this week" que no hizo. Verificado además que data/rock3-seed.json y tests/scenario-live-mode.json tienen people ["Brent","Bernardo"] exacto, así que ahí "Both" ≡ ["Brent","Bernardo"] y el cambio de esquema NO PUEDE mover una fecha: la regresión debe dar 60/60 plan y 17/17 live verdes SIN editar un solo expected-*.json, y un fixture tocado es señal de que algo salió mal. | Bernardo | Locked |
| D-106 | 2026-08-19 | R1 (September Challenge, owner Miguel) cargado en sprint-plan.json en el commit 45d7903: 4 proyectos, 11 milestones, 21 tareas, 12.25 días de trabajo. Cuatro resoluciones al cargarlo. (a) people[] pasa a ["Brent","Bernardo","Miguel"]; el string es Miguel, no Miguel Salas como proponía el borrador — confirmado por Bernardo contra la pestaña People. owner ∈ people es error bloqueante UNKNOWN_OWNER en validate.js, así que sin esa edición no cargaba NI R1 NI R3. (b) Las tres tareas que Miguel devolvió sin estimar (R1-M9-t1, R1-M9-t2, R1-M10-t1) las estimó BERNARDO, no Miguel: 1 / 1 / 0.5 días, más waitDays: 3 en R1-M10-t1, que Miguel había puesto en 0 siendo outreach a 10 afiliados. Decisión explícita de no consultarle para no perder tiempo; se corrige con Miguel si resulta mal. (c) R1-M8-t3 (outreach de Brent) venía en workDays: 0 porque Miguel no puede estimar trabajo ajeno: fijado en 0.5 por Bernardo. (d) La dependencia inferida R1-M8-t4.dependsOn perdió R1-M8-t3: el borrador infirió que asignar los vouchers esperaba el outreach de Brent, pero Miguel marcó t4 hecho y t3 no, o sea la realidad la desmintió; queda solo R1-M7-t1. Se aclara además quién commitea: sprint-plan.json va por PUSH, no BUILD — las instrucciones del proyecto dicen que PUSH escribe solo /docs, pero D-100 asigna el commit del archivo a PUSH y el Decision Log supersede. | Único bloqueante real del dashboard: con solo R3 cargado, Miguel no tenía nada que marcar. Verificado con el validate.js real del repo corrido en Node contra el archivo ensamblado, no con una reimplementación: ok: true, 0 errores, 0 warnings, 111 ids únicos, 0 dependencias sin resolver, 0 ciclos. El archivo commiteado se verificó pineado al hash 45d7903 y resultó byte a byte idéntico al producido. Confirmado en producción el mismo día: los badges Done de R3 siguen resolviendo, o sea que ningún evento del Sheet quedó huérfano. Consecuencia operativa a no confundir con bug: status no va en el JSON (§2), así que las 15 tareas que Miguel ya hizo entran en open, R1 arranca en 0% y su proyección se estira hasta el 2026-10-05 con DEADLINE MISSED en R1-M11; se recoge sola cuando Miguel marque. Revisar si el deadline del 5 de septiembre sigue incumplido DESPUÉS del marcado — ahí sí sería un problema de plan, no de estado. | Bernardo | Locked |
| D-105 | 2026-08-19 | Granularidad de workDays: múltiplos de 0.25 día en todos los tramos (0.25, 0.5, 0.75, 1, 1.25, …), piso 0.25, redondeo SIEMPRE hacia arriba. Se descarta usar 0 para tareas de minutos: workDays: 0 queda reservado a la espera pura del §7 (waitDays > 0). Aplicada a R1: 12 tareas subidas de valores entre 0.02 y 0.13 a 0.25 (0.67 → 3.00 días). R3 NO se recuantiza. | Miguel devolvió duraciones de dos decimales que parecen calculadas desde minutos, con granularidad inconsistente: 1 día para diseñar la mecánica de vouchers y 0.05 (≈24 min) para el análisis de CAC. La regla no se inventó: se verificaron las 47 tareas de R3 en producción y ya respetan exactamente esta grilla (0.25×5, 0.5×20, 1×15, 1.5×3, 2, 3, 5), sin valores fuera de grilla y sin un solo 0 — la regla describe retroactivamente lo cargado, así que R3 no se toca y su baseline y la regresión 60/60 de plan mode quedan intactas. El 0 se descartó por tres razones: reintroduce a propósito el defecto del 0/0 de Brent (una tarea abierta con peso 0 es invisible en la barra y en capacidad), R3 no tiene ninguno, y el 0 ya significa otra cosa en el §7 — un valor con dos sentidos hace el archivo ilegible. Medido: piso 0.25 da 61.5% de progreso en R1, el 0 daría 48%. Se eligió redondear hacia arriba por prudencia explícita de Bernardo: los planes se desvían por arriba. El tramo (0.5, 0.75] donde una grilla regular difiere de un ladder 0.25→0.5→1 resultó vacío en los datos, así que adoptar la regla simple no cambió ningún valor. | Bernardo | Locked |
| D-104 | 2026-08-19 | R1 entra a S3-2026 con ids PREFIJADOS (R1-P1, R1-M1, R1-M1-t1), estrenando la convención de D-099 un sprint antes de lo previsto. El sprint queda con dos convenciones conviviendo: R3 pelado (M2, M2-t1), R1 prefijado. R3 no se renumera. R2 seguirá el mismo esquema prefijado. | El borrador de R1 usaba P1-P4 y M1-M11, que chocan uno a uno con los P1-P6 y M1-M23 de R3 en producción: la colisión era total y el archivo ensamblado no validaba, así que elegir esquema no era opcional. Se evaluó seguir la numeración sin prefijo (R1 → P7-P10, M24-M34) y se descartó porque el M9 que Miguel tiene impreso pasaría a M32 y en el L10 nadie podría cruzar su documento con la pantalla, más 21 renumeraciones a mano. Se evaluó prefijar R3 también y se descartó por destructivo, no por estética: el Sheet ya tiene eventos de status apuntando a M2-t1, M5-t3, etc., y el §1 une JSON y estado por id, así que renumerar R3 huérfana todo el estado marcado en producción. No contradice D-099: esa fila prohíbe REESCRIBIR S3 y declara la convención como regla de productor que validate.js no verifica ni debe verificar — el validador solo exige unicidad sprint-wide, que es lo que el prefijo garantiza por construcción. Consecuencia cosmética verificada en el código, no supuesta: los ids SÍ se imprimen en pantalla (todos.js línea 792, etiqueta rock · task-id; línea 789, aviso "Blocked by …"), así que una fila de R1 se lee R1 · R1-M9-t1, con el Rock nombrado dos veces. No viola D-103: esa fila prohíbe códigos del spec (§, D-0XX) en texto que una persona lee, no identificadores de entidades, que ya se renderizan hoy para R3. | Bernardo | Locked |
| D-103 | 2026-08-19 | La regla de CLAUDE.md "plain language, no internal codes, in everything a person reads" se extiende explícitamente a DOS casos que no estaban cubiertos y que la Fase 9 violó: (a) los atributos title de tooltip, que son texto que la misma persona lee en la misma pantalla al pasar el mouse, y (b) las palabras de implementación donde el lector solo necesita la cosa — "regenerating the JSON" pasa a "regenerating the plan". El barrido que cerró esto se hizo extrayendo TODOS los string literals de issues.js, todos.js, board.js y app.js y buscando códigos solo ahí, en vez de grepear líneas: grepear no distingue un string renderizado de un comentario al final de una línea de código, y ese es exactamente el chequeo que hay que correr antes de cada L10. Los § y D-0XX en comentarios se quedan — son para nosotros. | Encontrado el 2026-08-19 preparando capturas de pantalla del dashboard en producción para el documento del leadership team: el subtítulo de la vista de Issues imprimía "Oldest first — age is the whole ordering rule (§13.3)". Nueve strings renderizados en total, no uno: la Fase 9 y la 8 parte 2B habían dejado códigos del spec en notas de la vista, el aviso de capacidad, la nota de confirmar la semana, la cascada de cancelación y cuatro tooltips del Sprint Board. Corregidos en c93a29a, versión de assets subida a 20260816g en los once assets, regresión 704 verde sin cambios, 60/60 plan y 17/17 live intactos, cinco archivos de código byte a byte idénticos — verificado contra los archivos commiteados y no contra el reporte de BUILD (precedente D-056). Detectado por un lector humano mirando la pantalla, no por ninguna suite: el mismo hueco de revisión que D-097 ya había registrado. | Bernardo | Locked |
| D-102 | 2026-08-19 | El documento de entrenamiento del leadership team se produce SOLO EN INGLÉS, y se descarta la versión en español. Motivo: se comparte en el canal de Leadership, que es un canal en inglés, y mantener dos versiones del mismo documento vivo garantiza que una quede atrasada. Aplica también al mensaje de anuncio. | Se había planeado español con traducción al inglés para Emery; Bernardo lo corrigió al ver el primer borrador. El documento queda en Leadership Hub en Drive, incluye capturas del dashboard en producción y una sección nueva que explica en lenguaje llano que a partir del próximo sprint el plan del Rock lleva días por tarea y no fechas, y que el dashboard deriva las fechas de ahí. | Bernardo | Locked |
| D-101 | 2026-08-19 | El L10 se reordena: connecting → wins → headlines → scorecards → estado de Rocks → revisión de to-dos → issues → armado de los to-dos de la semana siguiente. Headlines pasa de la posición 5 a la 3, inmediatamente después de wins, para no cortar la reunión por la mitad. Además se fija como REGLA que el dashboard tiene que reflejar la realidad ANTES de la llamada, no durante: cada persona marca todo su estado (terminado, en progreso, no terminado) y carga sus to-dos de la semana siguiente antes del L10, y en el paso 8 solo los PRESENTA. La llamada deja de ser donde se decide qué se hace. Como soporte operativo, Bernardo publica un recordatorio en el canal de Leadership una hora antes de cada llamada. Nada de esto cambia el código: el §11.5 ya permite cargar la semana por adelantado (selector Next week, D-092) y el paso 8 sigue siendo el único momento de confirmación (D-081a). Es un cambio de proceso, no de producto. | Salió al producir el documento de entrenamiento: escribir el paso 8 obligó a decidir si la semana se arma en la reunión o antes. Se elige antes por dos razones — el §11.5 dice explícitamente que el razonamiento de prioridad y adelanto pasa fuera de la reunión, cada dueño con su Rock Project (D-088, D-091), y una reunión de cinco personas armando semanas en vivo consume el tiempo que el IDS necesita. El reordenamiento de headlines lo pidió Bernardo por experiencia de las reuniones anteriores. | Bernardo | Locked |
| D-100 | 2026-08-19 | Reparto del `sprint-plan.json` en la cadena de Sprint Planning, en tres roles. (a) El Rock Planner emite el FRAGMENTO de su Rock: un elemento de `rocks[]`, nunca un archivo completo ni el sobre. (b) El proyecto de Sprint (nuevo, dedicado solo al sprint) construye el SOBRE — `schemaVersion`, `sprint{}`, `people[]`, que salen del Sprint Commitments y son hechos del sprint, no de ningún Rock — ensambla los fragmentos y corre la VALIDACIÓN CROSS-ROCK: unicidad de ids en todo el sprint y resolución de cada `dependsOn`/`crossDependsOn` contra el archivo ensamblado. Ninguna de las dos cosas puede hacerla un proyecto que planifica un Rock: desde adentro de un chat no se ven los otros Rocks. (c) El Rock Project Builder NO toca el JSON — monta la infraestructura del Rock y nada más. El commit del archivo lo hace la sesión PUSH. Además, el ensamblado cuenta los fragmentos recibidos contra la lista de Rocks del Sprint Commitments: un Rock cuyo fragmento nunca llega NO produce ningún error — el archivo valida, el dashboard carga y el Rock simplemente no existe, sin señal alguna. Ese conteo es lo único que lo detecta. | REEMPLAZA a D-014, que asignaba la emisión del JSON al Project Builder. D-014 se escribió cuando el Builder era el único proyecto que tocaba el plan estructurado; D-089 creó después el Rock Planner y le dio la planificación, dejando a D-014 apuntando a un mundo que ya no existe. El argumento que cerró la decisión es mecánico, no de preferencia: el Builder no tiene credencial sobre `F4LA/OperationsDashboard` — su propio `CLAUDE.md` lo confina al repo del Rock — así que aunque emitiera el JSON, el paso de ensamblaje y commit existiría igual. Emitirlo en el Planner además evita una transcripción: el Planner ya tiene el plan estructurado en el momento de crearlo, mientras que el Builder tendría que releerlo del documento. | Bernardo | Locked |
| D-099 | 2026-08-19 | Convención de IDs prefijados por Rock (`R1-M1`, `R1-M1-t1`) para milestones y tareas, vigente de S4-2026 en adelante. Es una CONVENCIÓN DE PRODUCTOR, no una regla del validador: vive en el Rock Plan template y en las instrucciones del Rock Planner, y `validate.js` NO la verifica ni debe verificarla. El validador sigue exigiendo solo unicidad sprint-wide, que ya alcanza — una colisión revienta al cargar de todos modos. S3-2026 se queda exactamente como está, con ids sin prefijo (`M2`, `M2-t1`), y no se reescribe. | Cada Rock se planifica en un chat separado que no ve a los otros, así que cinco Rocks planificados el mismo lunes producen cinco `M1` y el archivo ensamblado falla unicidad. El prefijo hace imposible la colisión sin que nadie coordine. Se evaluó subirla a `spec.md` como regla de formato validada y se descartó: el sprint que corre hoy ya está cargado en producción con ids sin prefijo, y una regla de formato lo invalidaría sin agregar ninguna garantía que la unicidad no dé ya. | Bernardo | Locked |
| D-098 | 2026-08-18 | UNA VISTA TIENE LISTENERS SI Y SOLO SI ESTÁ VISIBLE. Cada módulo de vista expone setActive(bool) que quita sus listeners antes de agregarlos (idempotente por construcción, así que llamarlo en cada render no puede apilar nada), y board.js tiene un único setActiveViewListeners() que enciende exactamente una vista y apaga las otras, llamado desde render(), donde el cambio de vista ya ocurre. NINGUNA vista invisible puede responder a un clic, se llame como se llame la acción. CAUSA QUE ESTO CIERRA: la Fase 9 agregó un segundo addEventListener sobre el mismo #main, y como todos.js e issues.js manejaban ambos la acción todo-add-adhoc, UN clic disparaba los DOS manejadores y se enviaban DOS createTask. Confirmado en producción, no en teoría: un solo clic creó T-0008 y T-0009 con createdAt 07:44:07 y 07:44:10 — tres segundos apartados porque el LockService del servidor los serializa, no porque hubiera dos clics. El guard de in-flight que ya existía NO protegía, porque los dos listeners ya estaban despachados para el mismo evento cuando el primero deshabilitó el formulario: deshabilitar no cancela listeners en cola. El barrido mostró que había SIETE listeners de TRES módulos sobre el mismo #main y que solo una acción colisionaba, por la casualidad de que los demás nombres no se repiten — o sea suerte, no diseño. Se descartó un despachador delegado único: obligaría a cada módulo a exportar sus handlers privados para que board.js los ruteara, y board.js igual necesitaría su propia rama, así que el caso especial sobrevivía. Se descartó explícitamente un "if (viewIsActive)" por rama: arregla la acción que se rompió y deja la próxima igual de rota. El test tests/listener-isolation.test.js cuenta ESCRITURAS y no comportamiento del handler, e incluye un check que rompe el invariante a propósito y confirma que el mismo clic entonces duplica — está probado capaz de atrapar el bug que vigila, no solamente verde. | Encontrado por Bernardo probando la vista nueva en producción el día del despliegue: creó un to-do desde un issue y aparecieron dos filas en la Sheet. El diagnóstico salió de las marcas de tiempo, que descartaban el doble clic. | Bernardo | Locked |
| D-097 | 2026-08-18 | Nace .btn-primary: acción primaria sobre el CUERPO CLARO de la página, relleno --steel-700 con texto blanco. .btn-secondary queda declarada como EXCLUSIVA DE LA BARRA OSCURA, con un comentario en styles.css explicando el fallo para que no se repita. CAUSA: .btn-secondary es blanco sobre blanco translúcido, que solo resuelve contra un fondo oscuro; sobre el cuerpo claro compone a ~#F6F8FB y la etiqueta queda en 1.06:1 — el botón está ahí, funciona, y es invisible. El faltante de fondo era que no existía una clase primaria para el cuerpo claro, así que se alcanzó la de la barra oscura. Números medidos y verificados de forma independiente en este chat, no tomados del reporte: 6.89:1 la etiqueta, 6.42:1 el bloque contra --bg (supera el 3:1 que necesita un borde no textual sin depender del borde), 4.83:1 en hover. Cero colores ni tokens nuevos: reusa el acento de marca que ya llevan links y estados activos. EL SEGUNDO BOTÓN IMPORTA MÁS QUE EL PRIMERO: el barrido encontró el mismo defecto en "Confirm this week" de todos.js, que es LA ÚNICA ACCIÓN DEL PASO 8 del §11.5 — el botón que congela el denominador de la semana. Si llegaba así al primer L10, el equipo habría armado la semana completa y nadie habría encontrado el botón para confirmarla. HUECO DE REVISIÓN REGISTRADO: el skill de accesibilidad no lo detectó porque audita el markup NUEVO, y este botón reusa una clase que era correcta en su contexto original. El defecto no vive en el elemento ni en la clase, vive en la combinación elemento × fondo, que es justo lo que una revisión por elemento no mira. | Bernardo lo encontró a ojo en producción: no veía el botón de levantar un issue. La causa la trazó Claude contra styles.css. | Bernardo | Locked |
| D-096 | 2026-08-16 | Se especifica el §13 (Issues) COMPLETO y se construye antes del primer L10 real, revirtiendo el diferimiento que lo mantenía sin especificar hasta después de usar la Fase 8 en vivo. El diferimiento se cae por dos razones: el primer L10 es en cuatro días y Bernardo necesita la lista de issues EN esa reunión, así que esperar significaba correr el L10 sin la vista y decidir después — al revés; y el alcance real es menor de lo que parecía, porque el §2 ya especifica el modelo de datos de un issue, la pestaña Tasks ya guarda sourceIssueId, y el backend ya lo acepta y lo escribe (hoy sin validar, con un comentario diciendo "§13 doesn't exist yet"). Se rechaza además el recorte que Claude había propuesto (dejar el seguimiento del to-do generado para un segundo pase): sin ese lector, sourceIssueId queda siendo un campo que el servidor escribe y nadie lee — exactamente el defecto estructural que D-080 encontró con la pestaña Tasks a mitad de la 2B — y el costo es un cruce sobre datos que ya van a estar en memoria, no una máquina nueva. DECISIONES DE FORMA: (a) los issues viven en su PROPIA PANTALLA, tercer segmento del control junto a Sprint board y To-dos, porque el §2 dice que se levantan durante la semana y no solo en la reunión (la vista tiene que servir sola) y porque el paso de IDS del L10 es un paso distinto del de to-dos; (b) CREAR UN TO-DO DESDE UN ISSUE Y RESOLVER EL ISSUE SON ACTOS SEPARADOS, no un solo botón: el §2 dice que un issue puede producir más de un to-do, así que atarlos forzaría a resolver al crear el primero, y de paso desaparece cualquier problema de atomicidad entre dos escrituras; (c) SIN RANKING MANUAL de issues: la lista se ordena por antigüedad, del más viejo al más nuevo, y muestra la edad — un issue de tres semanas encabeza solo. Se evaluó un campo de prioridad al estilo IDS y se descartó como perilla que nadie mantiene; revisable si un L10 real lo pide. | Bernardo hizo pushback sobre el diferimiento y después sobre el recorte, y tenía razón en las dos. Claude venía defendiendo "diferir hasta ver el sistema en uso" en varias decisiones del mismo día; acá ese razonamiento se dio vuelta solo, porque el uso real es en cuatro días y es justo donde los issues hacen falta. | Bernardo | Locked |
| D-095 | 2026-08-16 | Se restituye la acción de QUITAR UNA TAREA DE LA SEMANA, corrigiendo D-091(b), que la había eliminado. El razonamiento de D-091(b) —"si nada entra solo, no hay nada que sacar"— era equivocado: lo que desaparecía era el caso de sacar algo que el SISTEMA puso, pero una tarea agregada a mano en una pantalla compartida durante el paso 8 es exactamente el escenario del clic errado que motivó D-069 a construir pares de reversión para descartar y cancelar. Y con la semana abriendo vacía (D-091) agregar a mano pasó de ser excepcional a ser la ÚNICA forma de armar la semana, así que la frecuencia del error subió, no bajó. Forma que toma: no es "sacar la propuesta del sistema" sino DESHACER TU PROPIA ADICIÓN, ofrecida únicamente mientras la semana NO está confirmada (modo armado). Una vez confirmada la semana, sacar una tarea ya no es una corrección sino un cambio de compromiso, y para eso ya existen mover, descartar y cancelar con motivo obligatorio (§11.4). Mecánicamente es el unpin que ya existe, vuelto alcanzable desde la vista. | Reportado por BUILD como pregunta abierta al cerrar el pase de la reapertura (commit 75a925a) — conducta correcta, y el vacío era real. El error de razonamiento fue de Claude al redactar D-091(b), no de la ejecución: BUILD siguió la decisión al pie de la letra y después señaló que dejaba sin salida un clic errado. | Bernardo | Locked |
| D-094 | 2026-08-16 | Agregar una tarea ad-hoc queda disponible en cualquier semana NO CERRADA — es decir, en modo armado y en modo ejecución — pero NO en una semana ya cerrada. Corrige D-091(c), que decía "cualquier día, en cualquier semana, sin importar el estado de confirmación" y fue aplicado literalmente: el formulario aparecía también en la semana cerrada, donde crear una tarea la pinea a una ventana que ya terminó, produciendo trabajo comprometido en el pasado. La intención de D-091(c) era el caso del trabajo imprevisto que llega un martes, con la semana ya confirmada y en ejecución; ese caso se conserva entero. Una semana cerrada es solo lectura para la creación: si el trabajo apareció después de que la semana terminó, pertenece a la semana en curso, no a la que pasó. | Reportado por BUILD como pregunta abierta (commit 75a925a). Defecto de redacción de Claude en D-091(c): "cualquier estado" incluía un estado que no debía incluir. | Bernardo | Locked |
| D-093 | 2026-08-16 | Se cierran sin cambio de comportamiento las otras dos preguntas abiertas del pase de la reapertura. (a) EN MODO ARMADO NO HAY DROPDOWN DE ESTADO NI ACCIONES DE FILA, y eso es correcto, no una omisión: si nadie confirmó la semana no existe un compromiso contra el cual medir, y permitir marcar algo como hecho ahí crearía trabajo terminado fuera de toda semana medida — el denominador se congela al confirmar (D-072a, D-078) y una tarea marcada antes de que exista denominador no tiene dónde contarse. Marcar estado empieza cuando la semana entra en ejecución. (b) NO SE OFRECE RE-CONFIRMAR UNA SEMANA. Se sigue de D-092 (confirmar es una acción del modo armado y una semana confirmada ya no está en ese modo) y no hace falta ninguna otra protección, porque el §11.5 ya garantiza que lo agregado después de confirmar nunca infla el denominador congelado aunque sí cuente cuando se termina. Re-confirmar solo podría servir para re-congelar el denominador, que es precisamente lo que el diseño prohíbe. | Reportadas por BUILD como preguntas abiertas al cerrar 75a925a. Ambas resultaron consecuencias correctas del diseño y no vacíos: se ratifican por escrito con su fundamento para que nadie las reabra como si fueran omisiones. | Bernardo | Locked |
| D-092 | 2026-08-16 | El selector de semana del §11.1 pasa a TRES posiciones con etiqueta Y fecha juntas: `Closed · 15–21 ago`, `Current · 22–28 ago`, `Next week · 29 ago – 4 sep`. Y, la parte de fondo: QUÉ SE PUEDE HACER EN UNA SEMANA LO DECIDE SI ESTÁ CONFIRMADA (el evento confirmWeek), NO la posición del selector ni el día de la semana. Semana sin confirmar = modo armado (agregar tareas, contador de días, botón de confirmar). Semana confirmada = modo ejecución (marcar estado, mover, descartar, cancelar). Semana cerrada = revisión con el resumen del §12. SUPERSEDE PARCIALMENTE A D-084, que ratificó que "current" se comporta igual que "closing": eso es cierto seis días de siete y FALSO el viernes, que es el único día que importa, porque el viernes "current" es la semana que se está armando y no una que se revisa. DEFECTO REAL QUE ESTO CORRIGE, encontrado trazando thisweek.js y todos.js contra el commit 11b1d45: la ops week corre viernes→jueves y el L10 es viernes, así que el viernes offset 0 ("current") es la semana que arranca ESE DÍA — la que el §11.1 llama "opening" y que el paso 8 debe construir. Pero el código ataba el modo BUILD a offset +1, que el viernes es la semana que arranca el viernes SIGUIENTE. Resultado: en el primer L10 real, el único formulario de carga habría apuntado a la semana equivocada por siete días, y la semana correcta solo se podía mirar en modo revisión, sin poder agregar nada. Nadie lo habría notado hasta que alguien cargara sus tareas y no aparecieran. Anclar el modo a la confirmación elimina la clase entera de error: el jueves a la noche "next week" está sin confirmar y se puede cargar; el viernes esa MISMA semana pasa a "current", sigue sin confirmar, sigue cargable, y la fecha idéntica al lado de la etiqueta permite reconocerla al instante. La etiqueta relativa es segura porque "next week" dice literalmente respecto a qué es, así que es honesto que cambie de casilla al día siguiente; "opening" no lo decía y por eso se descarta ese nombre. | Salió de Bernardo pensando en voz alta sobre la regla de carga anticipada: primero corrigió "Closing" por "Closed" (el viernes esa semana ya cerró, no está cerrándose), después detectó que necesitaba poder cargar un jueves a la noche sin que el sistema se lo negara, y finalmente propuso "next week" como la etiqueta correcta. Claude había propuesto reducir a dos posiciones y se corrigió: tres posiciones con fecha visible es mejor porque cubre el jueves a la noche sin casos especiales. | Bernardo | Locked |
| D-091 | 2026-08-16 | La semana que se arma ABRE VACÍA. Se elimina el pre-llenado automático del §11.5 ("Proposed automatically: Rock tasks the live projection places in that window") y con él la frase "the system proposes, the people confirm", que pasa a ser "the people commit, the system counts". Nada entra al compromiso de una semana hasta que una persona lo pone ahí. Fundamento de Bernardo: un pre-llenado invita a decir "sí, sí, sí" sobre tareas que la persona no pensó, y la proyección solo sabe aritmética — no sabe qué está haciendo esa persona esa semana, ni qué necesita adelantar por una razón que solo ella conoce. Ese pensamiento pasa a ocurrir ANTES de la reunión, cada dueño con su propio Rock Project, que sí razona sobre prioridad, esperas y contexto (D-088 dejó firme que el Rock Project aconseja y el dashboard cuenta). Consecuencias: (a) lo que hoy se pre-llena pasa a ser un PANEL DE DISPONIBLES al costado, informativo, que sigue mostrando las bloqueadas marcadas y nombrando bloqueador y dueño (D-071b se conserva entero: esconder las bloqueadas esconde la coordinación); (b) "sacar de la semana" DESAPARECE junto con su pin — si nada entra solo, no hay nada que sacar, y se va una acción y un evento; (c) AGREGAR UNA TAREA AD-HOC DEJA DE ESTAR ATADA AL MODO DE ARMADO y pasa a estar disponible cualquier día, porque el trabajo imprevisto llega el martes y hoy el formulario solo existía mientras se armaba la semana; (d) el aviso de capacidad contra los 5 días laborales (D-081b) se vuelve MÁS importante, no menos: es lo único que le dice a la persona si se está cargando de más o de menos mientras suma tareas a mano. El paso 8 del L10 deja de ser planificación y pasa a ser coordinación, que es lo que justifica tener cinco personas en la misma llamada. | Bernardo lo planteó como principio ("el sistema no tiene que proponer nada") y Claude verificó contra el §11.5 committeado que el spec ya decía lo contrario y que todos.js lo implementaba con buildProposedForPerson. Se le advirtió explícitamente que esto reabre la parte 2B, cerrada el día anterior con 567 tests verdes, y Bernardo confirmó igual: mejor cambiarlo antes del primer L10 que después de que el equipo se acostumbre a una pantalla que invita a aceptar sin pensar. | Bernardo | Locked |
| D-090 | 2026-08-16 | El `deadline` de milestone (D-087) queda DEFINITIVAMENTE fuera del motor: no se propaga a las tareas del milestone ni entra en el orden de tareas listas del §4.4. Solo avisa. Se evaluó propagarlo para que el motor priorizara solo el trabajo de un milestone con compromiso externo y se descartó: mover trabajo sin que nadie lo pida es peor que avisar a tiempo, y el lugar donde se decide qué se hace esta semana es el L10 con el equipo, no el cálculo — decisión coherente con D-091, que saca al sistema de proponer trabajo. REGISTRADO PARA EVITAR UNA TRAMPA FUTURA: existen ahora dos campos de nombre casi idéntico y comportamiento opuesto — `hardDeadline` a nivel de TAREA del plan, preexistente, que SÍ entra en el orden del §4.4 como criterio (b) "earliest hardDeadline"; y `deadline` a nivel de MILESTONE, nuevo, que NO entra en ningún cálculo. Quien lea el schema rápido va a poner el campo en el nivel equivocado y obtener el comportamiento contrario al que espera. Se deja la asimetría documentada en el propio schema del §2 en vez de renombrar, porque renombrar `hardDeadline` tocaría el comparador del motor y sus fixtures congelados sin ganar comportamiento. | Encontrado por Claude revisando el commit 11b1d45 contra los archivos, no contra el reporte de BUILD (precedente D-056): al verificar que el motor no leyera el campo nuevo apareció el campo viejo de tarea, que sí lo lee. BUILD lo había detectado y reportado correctamente como preexistente y sin relación, y escribió además una prueba de equivalencia (dos corridas idénticas salvo el deadline, puesto sobre el milestone programado último) que sale byte-idéntica. | Bernardo | Locked |
| D-089 | 2026-08-16 | El §8 ("Sprint planning produces Rock-3-level detail every time") se ataca de raíz, con la causa localizada: el template de Rocks del proyecto Operating System NO TIENE CAPA DE TAREAS — llega hasta milestone, pide una target date por milestone, y manda las tareas a Asana ("Tasks go in Asana. Milestones stay here"). El Sprint Planning Meeting Playbook dice lo mismo por escrito ("Projects are defined in the meeting. Tasks are not", "Every milestone has an exact date"). Es decir: el Operating System no fallaba, llenaba fielmente un formulario que pedía lo contrario de lo que este spec necesita, y las instrucciones de ese proyecto ya decían la ley correcta ("dates are an output, not an input") pero el template las contradecía y el template gana. Se produjo un template nuevo (Leadership Team Rocks Template) con tabla de tareas de siete columnas — id, descripción, dueño, type, workDays, waitDays, dependsOn — más hard deadline opcional por milestone, cuttable y cut order, sin Asana y sin target dates, con el Rock 3 real como ejemplo lleno. El Sprint Planning se reestructura en tres bloques: (1) todo el leadership junto cierra Rocks con dueño y DoD, (2) cada dueño solo o en pareja convierte sus notas en el documento completo con duraciones — porque solo quien hace la tarea puede estimarla, y un comité de cinco estima peor y más lento, (3) todos juntos otra vez, cada dueño presenta y recibe feedback antes de trabar. Esto formaliza lo que funcionó en el offsite de Tampa y jubila el diseño de 3 asistentes del playbook. El bloque 2 gana un proyecto Claude dedicado ("Rock Planner"), separado del Rock Project Builder existente: el Planner PLANIFICA el Rock, el Builder MONTA su infraestructura, y la disciplina del Builder de no inventar contenido de Rock (§3.1 de sus instrucciones) queda intacta a propósito. | Bernardo subió el template real y el playbook real; ambos se leyeron completos antes de concluir. Se confirmó que el Rock Project Builder ya exigía en su Stage 1 exactamente los campos que el template nunca dio (owner, duration en working days, depends on, y tareas con dueño): los dos proyectos venían operando contra contratos distintos desde antes de que el dashboard existiera. Bernardo confirmó que EOS/Traction sigue vigente en todo lo demás — Rocks, dueño único, DoD verificable, L10, IDS, VTO — y que lo que se jubila es una práctica, no el sistema. | Bernardo | Locked |
| D-088 | 2026-08-16 | La integración entre el dashboard y los Rock Projects se DESCARTA, no se difiere. El §8 la contemplaba como puente downstream (un status.json con los mismos ids de tarea) y se evaluó a fondo en este chat; se decide no construirla. Fundamento: solo podría sincronizar la mitad del problema — un chat puede depositar un "done" al cerrar una tarea, pero NO puede detectar in_progress, y el marcado manual en el dashboard tiene que seguir existiendo igual porque el resto del equipo (Emery, Joyce, los coaches) no trabaja en Claude Code. Se pagaría un endpoint de lectura nuevo, un cambio de spec y código, para automatizar un caso y medio. El segundo argumento a favor que se había planteado — que el Rock Project aconsejaría sobre datos viejos — es FALSO y así se reconoce: el estado que un Rock Project necesita vive en su Decision Log y su Project Brain, que se actualizan al cierre de cada chat por disciplina del setup, no por suerte. Reparto que queda firme sin integración: el dashboard es autoridad única sobre estado, fechas y capacidad de la persona a través de todos sus Rocks; el Decision Log de cada Rock es autoridad única sobre el porqué; cada Rock Project recomienda dentro de su propio Rock, y la ceguera de no ver los otros Rocks se corrige río abajo en el L10, donde el aviso de capacidad del §11.5 ya suma tareas de todos los Rocks más las ad-hoc contra los 5 días de la persona. REGISTRADO COMO COSTO ACEPTADO: en un Rock de dos dueños, si el otro dueño marca su tarea en el dashboard sin pasar por un chat, el Brain de ese Rock no se entera. Se acepta; si molesta en la práctica se resuelve entonces con datos reales de uso encima, no ahora. | Claude había recomendado primero quitarle al Rock Project el cálculo de fechas y el "qué sigue"; Bernardo lo rechazó con el argumento correcto (el Rock Project es el único que conversa, conoce el contexto y guía cuando algo está bloqueado — el dashboard nunca hace eso) y Claude se corrigió. Bernardo fue además quien identificó que una integración que solo cubre la mitad no se justifica. | Bernardo | Locked |
| D-087 | 2026-08-16 | Un milestone gana un campo `deadline` OPCIONAL en el §2: fecha ISO, la restricción impuesta desde fuera (un lanzamiento, un compromiso con un cliente, un vuelo), nunca una estimación. Es la contracara exacta de lo que se está eliminando: una target date es una respuesta inventada y sale del sistema; un hard deadline es un dato de entrada real y entra. Reglas: (a) va en el MILESTONE, no en la tarea, porque un deadline es del resultado verificable y no de un paso intermedio — las ad-hoc conservan su propio campo deadline sin cambios; (b) el motor NO lo usa: sigue calculando por duraciones, dependencias y capacidad, y el deadline no reordena ni prioriza nada, solo se compara contra el resultado; (c) si la fecha calculada del milestone lo pasa, se muestra un chip rojo con los días de diferencia, y el Rock que contiene un milestone vencido lo delata en su encabezado para no tener que abrirlo; (d) NO toca la burn-up ni el chip on-track — esos miden avance contra el plan congelado y el deadline mide otra cosa, así que mezclarlos pintaría de rojo un Rock que va bien en todo lo demás; (e) validación: si está presente y no es una fecha ISO real, error bloqueante (falla ruidosa, nunca ignorarlo en silencio); si cae fuera de la ventana del sprint, warning no bloqueante. | Encontrado al traducir el Rock de Miguel: M11 lleva "⚠ HARD DEADLINE: September 5" porque el September Challenge arranca el 7 y no se mueve, y el schema no tenía dónde guardarlo — el dashboard habría dicho que todo está bien mientras el portal llegaba tarde. Se construye ANTES de cargar R1 y R2: el template nuevo ya le pide el dato a la gente, y cargar sin el campo lo perdería en silencio. | Bernardo | Locked |
| D-086 | 2026-08-15 | Cierra la tercera pregunta abierta de la 2B: una tarea done ANTES de que la semana se confirme no puede entrar al array congelado, verificado rastreando el código y no asumido. Las tres únicas vías de entrada al array — buildProposedForPerson (vía rockBucketsForWindow), el dropdown de availableToPull, y el alta de ad-hoc — filtran o garantizan status === "open"; una tarea done nunca es candidata. No se escribe regla nueva porque el caso es estructuralmente inalcanzable, no meramente raro; si alguna vez se observa, es evidencia de un defecto en otra parte (una tarea done reproyectada como open), no una ambigüedad del §12. | Trazado al revisar el commit e67d20c: rockBucketsForWindow (todos.js) descarta explícitamente cs.status !== "open" antes de proponer; availableToPull ya listaba solo abiertas desde D-063(e)/D-071(b). | Bernardo | Locked |
| D-085 | 2026-08-15 | Cierra la segunda pregunta abierta de la 2B: createTask NO necesita una re-verificación propia del lado del cliente además de la que ya hace el servidor. El servidor ya ejecuta write-then-verify internamente (D-066e) y devuelve verified en una respuesta JSON legible en el camino normal — a diferencia de postEvent/verifyEvent, cuyo poll-loop del lado del cliente existe porque el POST de un evento puede caer en no-cors y volverse opaco. El único caso donde postCreateTask no puede leer la respuesta (TypeError → fallback no-cors) ya se maneja correctamente: se reporta como UNVERIFIED, nunca como éxito fingido, con instrucción de revisar la pestaña Tasks a mano — el mismo cuidado que D-046/D-047 fijaron para los eventos, aplicado acá sin necesidad de duplicar el poll-loop completo. | Trazado contra postCreateTask en dashboard/todos.js (commit e67d20c): el camino feliz lee una respuesta JSON normal del fetch; el fallback no-cors nunca se interpreta como confirmación. | Bernardo | Locked |
| D-084 | 2026-08-15 | Cierra la primera pregunta abierta de la 2B: el modo "current" del selector de semana se comporta IGUAL que "closing" (modo revisión: lista mixta, acciones solo en filas sin terminar, motivo obligatorio para descartar/cancelar, marcador de arrastre). Fundamento: las dos posiciones leen la misma lista congelada de la semana que está corriendo — la única diferencia entre ellas es el día en que se mira, no el contenido. "Closing" es esa misma semana vista el viernes del L10; "current" es la misma semana vista cualquier otro día antes de esa reunión, que es exactamente el escenario del chequeo de mitad de semana de Bernardo (miércoles) descrito en el contexto de la empresa. No hacía falta un tercer comportamiento porque no hay una tercera realidad que describir. | El build de la 2B había implementado esto y lo reportó correctamente como lectura propia, no como algo que el spec fijara — comportamiento correcto (D-076 exige reportar vacíos como abiertos), y se ratifica acá con el fundamento por escrito. | Bernardo | Superseded in part by D-092 |
| D-083 | 2026-08-15 | Se acepta MAX_NOTE_LEN duplicado en dashboard/config.js (valor 5000, idéntico al de backend/Code.gs) como excepción consciente a la letra de D-075 ("el frontend consume ese mismo número desde un solo lugar y nunca define uno propio"). El frontend es estático (GitHub Pages, sin build step) y Code.gs es Apps Script: no hay forma de que el cliente le pregunte al servidor su límite sin agregar una llamada RPC nueva solo para eso, y esa máquina nueva no se justifica por un contador visual. Se acepta el riesgo de desincronización (la misma cicatriz que citan D-024/D-088) porque el valor casi no cambia y queda documentado en un comentario en el propio config.js señalando que es la otra copia que hay que tocar si el límite del servidor cambia alguna vez. | Encontrado al revisar el commit e67d20c de la Fase 8 parte 2B contra los archivos reales, no contra el reporte de BUILD (precedente D-056). BUILD lo había dejado documentado en el comentario pero sin pasar por una fila del log — una decisión de diseño no se toma en silencio dentro de un commit aunque esté bien comentada. | Bernardo | Locked |
| D-082 | 2026-08-15 | La Fase 6 (View 2 — Network / Timeline / Gantt, §6) se CANCELA. No se difiere más: SUPERSEDE a D-059, que la había diferido con la precondición de cargar los 4 Rocks. La precondición perdió su sentido con D-060: la razón para esperar a los 4 Rocks era ver cuál Rock arrastra a cuál, y D-060 fijó que las dependencias NO cruzan Rocks — se entrelazan dentro de cada uno — así que los conectores cross-Rock que el §6 manda dibujar no van a tener nunca nada que dibujar. Lo que la timeline iba a contestar ya está contestado por otras dos pantallas que existen: "cuánto atrasados vamos" lo da el pie numérico obligatorio de la burn-up más el chip on-track (D-053), y "qué toca esta semana" lo da el §11. Ninguno de los dos rituales reales de la empresa — el L10 del viernes y el chequeo del miércoles — pide un Gantt. Consecuencias concretas: (a) el §6 "View 2 — Network / Timeline" y su entrada en la secuencia de build del §9 quedan supersedidos por esta fila, sin tocar la numeración de secciones del spec; (b) dashboard/network.js, que sigue siendo un stub vacío desde la Fase 0, SE BORRA junto con su <script> en index.html — un stub de una vista cancelada es una promesa que el repo hace y nadie va a cumplir; (c) el control segmentado queda definitivamente en dos segmentos, con lo que se cierra la migración a tabs que D-062 había aceptado como consecuencia probable de una tercera vista. LO QUE SE PIERDE, registrado a propósito para que no aparezca como sorpresa: la capacidad SÍ cruza Rocks (availableFrom es un único mapa por persona para todo el sprint, verificado en D-060), y una timeline habría sido la única pantalla donde se ve la cola de una persona atravesando sus dos o tres Rocks a la vez. Hoy eso no se ve en ningún lado y se acepta así. Si alguna vez molesta, se resuelve con una lista por persona, no con un Gantt. Los prototipos Rock3_Network_Graph.html y Rock3_Interactive_Timeline.html quedan referenciados en el Brain por si se retoma. | Bernardo lo planteó al revisar lo que quedaba pendiente: la timeline no le suma nada. Cancelar en vez de seguir difiriendo es deliberado — una fase diferida indefinidamente es basura en el plan, exactamente lo que D-068 dice de una tarea que se arrastra de semana en semana hasta que el sprint termina. | Bernardo | Locked |
| D-081 | 2026-08-15 | Diseño de la vista del §11 fijado antes de construir la Fase 8 parte 2B, elegido sobre mockup con datos reales del Rock 3 (patrón D-048/D-051/D-062). LAYOUT: Opción A — una ficha por persona, apiladas en vertical, con la tasa del §12 en el encabezado de cada ficha y la del equipo arriba de todo. Dentro de la ficha una sola lista con tareas de Rock y ad-hoc mezcladas per §11.3, riel azul e id (R3 · M17-t2) como marcador de origen. Seis vacíos que el §11 no cierra, resueltos acá: (a) CONFIRMACIÓN ÚNICA POR SEMANA, no por persona: un solo evento confirmWeek con clave WEEK-<lunes> tal como lo construyó la parte 1, y el botón de confirmar se muestra ÚNICAMENTE con el filtro de persona en Everyone. El flujo de la reunión es revisar persona por persona con el filtro puesto y confirmar una sola vez al final desde Everyone. Se evaluó y se descartó una clave por persona (WEEK-<lunes>-<nombre>): el argumento que la sostenía era que sin ella no se podía distinguir "no confirmó" de "confirmó vacío", y ese argumento es FALSO — D-078(a) mide a cada persona contra su porción del array congelado, así que semana sin confirmar da denominador null para todos y semana confirmada sin tareas de esa persona da denominador 0, que ya son estados distintos y se renderizan distinto. Como el beneficio no existía, no se paga su costo, que era reabrir la 2A ya cerrada en tres archivos (Code.gs, events.js, metrics.js) más un redespliegue. (b) CAPACIDAD = 5 días laborales, que es lo que contiene la ventana viernes→jueves; el aviso del §11.5 avisa y no bloquea. (c) EL SELECTOR DE SEMANA NO SE PERSISTE: se recalcula en cada carga, closing el día de inicio de la ops week y current cualquier otro día, per §11.1. Persistirlo haría que un miércoles se abra en una semana que cerró hace días. (d) LOS FILTROS DE PERSONA Y ORIGEN TAMPOCO SE PERSISTEN, arrancan siempre en Everyone / All por la misma razón: un filtro guardado esconde trabajo sin decir que lo esconde. Acting as sigue persistiéndose en opsdash.actor y la vista en opsdash.view (D-062), sin cambios. (e) EL CONTROL SEGMENTADO SIGUE CON DOS SEGMENTOS, "Sprint board" / "To-dos": el §11 SUPERSEDE al §6.3, no convive con él, así que la vista vieja se retira en el mismo pase. (f) UNA TAREA DESCARTADA O CANCELADA SIGUE VISIBLE en la lista de su semana, con su motivo y un botón de deshacer en la fila: D-069 creó los pares de reversión precisamente por el clic errado en pantalla compartida, y una fila que desaparece no se puede deshacer desde la vista. | Bernardo eligió A viendo el mockup y no en abstracto. Planteó además querer confirmar por persona, que es lo que llevó a evaluar la clave por persona y a descubrir que su justificación no se sostenía; al no sostenerse, la forma barata resultó ser también la correcta, y la parte 2A no se reabre. | Bernardo | Locked |
| D-080 | 2026-08-15 | El frontend gana un lector de la pestaña Tasks, sin mecanismo nuevo: TABS.TASKS en dashboard/config.js, parseTasks(rows) en app.js junto a parsePeople y parseSettings, cuarta llamada dentro del Promise.all existente y una clave más en el mount(). La fila de Tasks aporta SOLO lo estructural de una tarea ad-hoc (id, desc, owner, workDays, deadline, week, sourceIssueId) — es el equivalente del plan para las ad-hoc. El status NO sale de esa fila: se sigue derivando del fold de Events como el de cualquier tarea, así que no hay dos fuentes de estado. Se descartó que createTask escribiera además algo al Events log para reconstruir la tarea desde ahí: duplicaría el dato y extendería estructuralmente el Events log, que el §3 prohíbe. | Hueco estructural encontrado al trazar el código real antes de escribir el prompt de BUILD de la 2B, mismo procedimiento que produjo D-076. La parte 1 le enseñó al SERVIDOR a escribir una tarea ad-hoc en la pestaña Tasks (D-066) y la 2A le enseñó al CLIENTE a interpretar sus eventos, pero nadie leía la fila: config.js declara People, Events y Settings y ninguna otra, y no hay una sola referencia a la pestaña Tasks en app.js, board.js, events.js ni config.js. Sin esto la 2B es imposible por tres lados a la vez: el §11.3 pide una lista única con Rock y ad-hoc y faltaría la mitad; weeklyCompletion exige taskOwners y una ad-hoc no tendría dueño, así que el §12 mediría mal; y el aviso de capacidad del §11.5 suma workDays que viven solo en esa fila. | Bernardo | Locked |
| D-079 | 2026-08-14 | Cierra la última pregunta abierta de la Fase 8 parte 2A: una tarea que está en el commitment congelado de la semana pero que llegó a done ANTES de la ventana cuenta como completed. Hasta ahora quedaba en el denominador y fuera del numerador, así que bajaba la tasa de una persona por trabajo que estaba hecho. Se agrega la excepción isCommitted a la rama completed de classify() en metrics.js, igual que ya la tienen discarded, cancelled y moved. Tres razones. Primera, la asimetría: los otros tres resultados ya honran la regla "comprometida se juzga siempre" que fijó D-078 corrección 1, y completed era el único que quedaba afuera, justo el único donde la exclusión produce un número visiblemente falso. Segunda, cuál de los dos errores duele más: contarla puede inflar la tasa si alguien comprometió por error algo ya hecho, que es raro y sin consecuencia, mientras que no contarla hace que el dashboard le diga "no lo hiciste" a una persona sobre algo que está terminado y a la vista en la reunión — y ese es el error que hace que el equipo deje de creerle a la herramienta, con lo que se cae el §12 entero. Tercera, qué acota realmente el "within the window" del §12: acota ruido de AFUERA, que es el defecto que corrigió D-078, no niega crédito por algo que sí estaba comprometido. El commitment congelado ya es el filtro de pertenencia a la semana: si el id está en el array de D-070, es de esta semana por definición. | Reportada correctamente como pregunta abierta y no como decidida por la sesión de BUILD al cerrar el pase de correcciones de D-078 (commit 626557e). El caso es raro — comprometerse a algo ya terminado — pero el número equivocado aparece en pantalla en el momento de mayor exposición, que es el cierre semanal del L10 con el equipo mirando. | Bernardo | Locked |
| D-078 | 2026-08-14 | Cierra los cinco vacíos del spec que la Fase 8 parte 2A reportó como preguntas abiertas, más tres correcciones encontradas al revisar el código del commit c025459 contra los archivos reales (precedente D-056: se revisa lo commiteado, no el reporte). (a) DENOMINADOR POR PERSONA: cada persona se mide contra SU porción del commitment congelado — las tareas del array de D-070 cuyo owner es esa persona — nunca contra el denominador del equipo, que haría que nadie llegue nunca. Una tarea Both cuenta en el denominador de las dos personas, así que la suma de los denominadores individuales NO da el del equipo; es la misma lectura que D-063(b), donde un compromiso conjunto lo es de los dos. (b) UN RESULTADO POR TAREA, con prioridad fija completed > discarded > cancelled > moved. Sin exclusividad los cuatro contadores suman más que las tareas y la tasa deja de significar nada. La prioridad NO se ordena por timestamp, a propósito: es estable y no depende del orden de llegada de los eventos. El tramo discarded vs cancelled es inalcanzable en la práctica porque D-067 separa los namespaces por servidor, pero queda explícito. (c) QUÉ TAREAS SE JUZGAN EN LA SEMANA: el commitment congelado MÁS cualquier tarea cuyo evento de resultado caiga DENTRO de la ventana. Una tarea nacida y descartada la misma semana cuenta como descartada aunque nunca estuvo comprometida — es deseable, el §12 trata la tasa de descarte como un hecho sobre la reunión, y una tarea inventada y tirada el mismo viernes es exactamente el ruido del L10 que hay que ver. (d) CANCELAR UNA TAREA YA done: los work-days salen del numerador Y del denominador de progress() (§5.1). Se acepta con una guarda que vuelve el caso inalcanzable: la vista del §11.4 solo ofrece acciones sobre tareas SIN TERMINAR, así que la 2B no muestra cancelar ni descartar en una fila done. Sin esa guarda, cancelar trabajo ya hecho haría BAJAR el porcentaje de avance, que es absurdo. (e) CANCELADA Y DIFERIDA A LA VEZ se reporta como cancelada. CORRECCIÓN 1 (defecto): classify() acota por ventana solo completed; discarded, cancelled y moved preguntan únicamente por pertenencia al mapa. Como el set considerado incluye toda tarea con cualquier evento de status de toda la historia, una tarea descartada hace tres semanas se seguiría contando como descartada de esta semana y de todas las siguientes, y una pineada a una semana lejana figuraría como movida cada semana hasta llegar. Los cuatro resultados se acotan a la ventana usando el timestamp del evento, que discards() y cancels() ya devuelven. CORRECCIÓN 2: con denominador [] (semana confirmada vacía a propósito) rate deja de ser 0 y pasa a null. Con denominador cero la tasa es indefinida, no cero; mostrar 0% a alguien que completó tres tareas agregadas a mitad de semana es la misma clase de mentira que el denominador silencioso que D-070 existe para impedir. CORRECCIÓN 3: las dos salidas tempranas de error de liveMode devuelven deferredTasks pero no cancelledTasks ni stats.cancelled; se completan, porque la 2B va a leer .cancelledTasks.length y hoy reventaría contra un resultado de error. | Las cinco preguntas llegaron correctamente reportadas como abiertas y no como decididas, que es la conducta que el prompt pedía. Las tres correcciones salieron de leer los archivos del commit y no el reporte: la 1 es un defecto real de conteo que se agrava con cada semana que pasa, la 2 y la 3 son bordes que la 2B habría encontrado en pantalla. La 2A queda por lo demás verificada: board.js, app.js e index.html byte a byte idénticos a e84f718, versión sin subir en 20260814b, plan mode inmune por firma (el call site de collectActive en planMode se sigue llamando con dos argumentos), y la cobertura de cancel en un archivo aparte para no tocar lo que arbitra expected-live-mode.json. | Bernardo | Locked |
| D-077 | 2026-08-14 | ACLARA D-071(c) y CORRIGE EL REGISTRO DE D-076. Las dos filas se pisan sobre dónde vive la matemática del §12: D-071(c) la puso en dashboard/todos.js junto al markup del §11, y D-076 la asignó a metrics.js sin marcarlo como supersesión — un error de redacción de esta sesión, no una decisión tomada. Se resuelve a favor de metrics.js, y el fundamento es D-053, que fijó el patrón de la casa con todas las letras: la matemática vive en metrics.js como función pura y el markup en el archivo que dibuja. La matemática del §12 (completadas, movidas, descartadas, canceladas, tasa contra el denominador congelado de D-070) es de la misma familia que el §5.1 y burnupSeries, que ya viven ahí. Consecuencia operativa: dashboard/todos.js queda como archivo de MARKUP PURO del §11, y la parte 2A no lo necesita para existir. D-071(c) queda Locked pero parcialmente inexacta en ese punto; el resto de D-071(c) — que todos.js se crea, y que los renderTw* obsoletos de board.js se retiran — sigue vigente sin cambios. | Detectado al trazar el código para escribir el prompt de BUILD de la 2A, antes de que se escribiera una línea. Poner la matemática en todos.js metería matemática y markup en un mismo archivo, que es justo lo que D-053 separó, y ataría toda la 2A a que todos.js exista, cuando el sentido de partir en 2A y 2B (D-076) es que la 2A sea verificable entera por regresión sin abrir el navegador. El único argumento a favor de todos.js era la proximidad con la vista que consume el dato: conveniencia de lectura, no principio. Vale como precedente del mismo tipo que D-056: una fila del log que contradice a otra en silencio es peor que no tenerla. | Bernardo | Locked |
| D-076 | 2026-08-14 | La Fase 8 parte 2 se parte en dos pases, igual que se partió la Fase 8 en backend y frontend. 2A = lógica pura y tests, sin tocar una sola pantalla: las proyecciones nuevas del fold en dashboard/events.js, el camino de cancel en engine.js, las extensiones de thisweek.js (opsWeek con offset y el contrato nuevo de availableToPull) y la matemática del §12 en metrics.js. 2B = la vista: dashboard/todos.js, el retiro de los renderTw* de board.js, el wiring en app.js y el bump de versión de D-057. Fundamento: es la misma separación matemática/markup que ya fijaron D-053 y D-071(c), 2A es verificable entera por regresión sin abrir el navegador, y 2B se construye sobre una base ya verde en vez de sobre código que se está escribiendo al mismo tiempo. Punto de entrada del cancel fijado al trazar el motor: collectActive() (engine.js línea 139) recibe un set opcional de tareas canceladas que se le pasa ÚNICAMENTE desde liveMode (línea 626), nunca desde planMode — así una cancelación sale del schedule por el mismo camino que un deferred del plan (D-068c) y el fixture de plan mode ni se entera. El bump de versión pertenece a 2B: 2A toca .js pero nada de lo que toca se consume todavía desde una pantalla. | Se trazó el código real antes de escribir el prompt de BUILD y apareció un hueco que no estaba en ningún pendiente: dashboard/events.js proyecta hoy solo setStatus (línea 342), setDeliverable (369) y pins (380). No existe proyección de discard, cancel ni confirmWeek. El backend de la parte 1 sabe ESCRIBIR esas acciones, pero el cliente que las LEE no las conoce, así que D-067 ("discarded es un estado derivado de la presencia del evento en el fold") no tenía de dónde derivarse y el denominador congelado de D-070 no era legible. No es mecanismo nuevo: el fold ya indexa por (Task ID, Action), y pins() (líneas 380-401) ya resuelve el par positivo/negativo con desempate por rowIndex, que es exactamente la forma que D-069 mandó copiar. El hallazgo es lo que fijó el contenido de 2A. | Bernardo | Locked |
| D-075 | 2026-08-14 | Cierra el punto que D-072 dejó abierto a propósito: MAX_NOTE_LEN = 5000 pasa a aplicarse a TODO Note, no solo a confirmWeek. El chequeo de largo se mueve al principio de validateNote_(), antes de las ramas por acción, reusando el código NOTE_TOO_LONG ya existente con el mensaje generalizado (deja de nombrar a confirmWeek). Cero maquinaria nueva: la constante y el código de error ya existían, solo cambia dónde se aplican. El frontend de la parte 2 consume ESE MISMO número desde un solo lugar y nunca define uno propio. Prohibido `maxlength` en el input de la razón: un pegado que entra cortado sin avisar es la misma truncación silenciosa que D-070 declaró inaceptable, movida al navegador. La forma es contador visible más botón de confirmar deshabilitado nombrando por cuánto se pasa. El recorte del Note al mostrarlo en las filas del §11.4 y del §12 es asunto de render (clamp de CSS con expandir), nunca del dato guardado. | Trazado contra backend/Code.gs del commit 8333032: validateNote_ (línea 762) exige razón en discard y cancel pero el tope de largo vive DENTRO de la rama de confirmWeek, así que un pegado enorme en una razón llegaría a una celda de Sheets que corta a 50.000 y daría una excepción cruda en vez de un rechazo nombrado. Se descartó un tope de servidor más chico (tipo 500): el servidor topea para proteger la celda, no para modelar cuán larga debe ser una razón — eso es decisión de producto y vive en el frontend. Se descartaron dos números distintos (5000 servidor / 500 frontend) por la cicatriz que D-024 cita como D-088 del testimonial-dashboard: dos copias del mismo valor se desincronizan y después nadie sabe cuál manda. Como efecto colateral quedan cubiertos los notes opcionales de pin, unpin, undiscard y uncancel, que hoy también estaban sin tope. | Bernardo | Locked |
| D-074 | 2026-08-14 | Regla del smoke test: assertNoRow detecta EXISTENCIA de una fila con ese Task ID en la cola de Events, no NOVEDAD, así que todo caso de rechazo que la use debe pasarle un id único por corrida. Corolario: cuando el namespace obliga a un id fijo (T-NNNN son cuatro dígitos exactos; WEEK- es un lunes ISO), la unicidad por corrida es imposible por construcción y esos casos NO deben recibir opts.taskId — deben saltear la verificación en vez de "completarla". Queda documentado en el comentario de la función. | Un caso usaba el id real M2-t1 y falló contra eventos de fases anteriores que ya estaban en el Sheet: el rechazo había funcionado bien y el detector de fugas se disparó contra datos viejos. La auditoría encontró otras dos con la misma forma (T-0001, que es literalmente el primer id que asigna createTask, y WEEK-2026-08-17, que el happy path escribe de verdad); no rompían todavía solo porque no pasaban opts.taskId. Corregidas preventivamente a un centinela T-9999 y a WEEK-2026-09-07. | Bernardo | Locked |
| D-073 | 2026-08-14 | Aclaración a D-066(a): "un id nunca se reusa" aplica a los ids que llegaron a una fila persistida en Tasks. Un id quemado por una escritura fallida (el pin entró, la fila de Tasks no) SÍ se reasigna en el intento siguiente, y es inofensivo: el pin huérfano apunta a un id sin fila y nunca se une en el fold, y cuando ese id se reasigne, el createTask nuevo escribe su propio pin que pliega después por Timestamp y gana. | El mensaje de TASK_ROW_APPEND_FAILED dice "the id will be reassigned", que contradice la letra de D-066(a). El código es correcto; el texto del log era el que estaba mal redactado. | Bernardo | Locked |
| D-072 | 2026-08-14 | Ratificadas las cuatro decisiones que BUILD tomó por vacío del spec en la parte 1: (a) un confirmWeek sin Note se RECHAZA, nunca se lee como lista vacía — una semana sin compromisos manda "[]" explícito, porque un denominador vacío por accidente y uno vacío a propósito tienen que verse distinto (mismo espíritu que la prohibición de truncar en D-070); (b) BAD_VALUE_WEEK se agrega como código propio: un week presente pero no lunes reportaba MISSING_WEEK, mensaje activamente engañoso; (c) MAX_NOTE_LEN = 5000, aplicado SOLO a confirmWeek, que es donde D-070 lo pide; (d) TASK_ROW_APPEND_FAILED como código del caso en que el pin entra y la fila de Tasks no. QUEDA ABIERTO, deliberadamente: el Note de discard y cancel sigue sin tope, contra una celda de Sheets que corta a 50.000 caracteres — un pegado enorme daría una excepción cruda en vez de un rechazo nombrado. Se resuelve en la parte 2, donde el frontend va a limitar el input de todas formas. | Backend de la Fase 8 (commit 5262844). Las cuatro se revisaron contra el código real y la regresión, no contra el reporte. Procedimiento manual (C) ejecutado el 2026-08-14: dos createTask en paralelo devolvieron T-0005 y T-0006, filas 6 y 7, ambas con su pin y verified:true. Los timestamps quedaron separados por ~3 segundos y la segunda petición lanzada respondió primero — el LockService serializó en vez de dejar que las dos leyeran el mismo máximo de la columna y asignaran el mismo id. | Bernardo | Locked |
| D-071 | 2026-08-14 | Reparto de código de la Fase 8. (a) dashboard/thisweek.js SE EXTIENDE, no se reescribe: opsWeek recibe un offset opcional (-1 / 0 / +1 → semana que cierra / actual / que abre) manteniendo válida su firma actual; buckets sobrevive como la matemática de medición del §12 aunque su render de tres columnas se retire; cascadeOf queda intacta y pasa a alimentar también la previsualización de cancelación (D-068). (b) availableToPull CAMBIA DE CONTRATO por el §11.5: debe devolver también las tareas bloqueadas, marcadas y nombrando su bloqueador y el dueño del bloqueador. Esto SUPERSEDE a D-063(e), que decía exactamente lo contrario. Las aserciones de tests/thisweek.test.js que verifican que una tarea bloqueada NO aparece deben reescribirse para verificar que aparece marcada — es un cambio de expectativa por decisión de spec, no un test aflojado para que pase. (c) Se crea dashboard/todos.js para el markup de la vista §11 y la matemática del §12; las funciones renderTw* de board.js que el §11 deja obsoletas se retiran. Motivo: board.js ya pesa 56 KB y el patrón de la casa (D-053) separa matemática de markup, no obliga a que todo el markup viva en un solo archivo. (d) La burn-up NO se toca salvo por lo que exige D-068(e). | Pregunta abierta del handoff de Fase 8. Se trazó el código real antes de decidir: las cuatro funciones puras de thisweek.js envejecieron bien y solo una cambia de contrato, así que reescribir habría tirado 67 tests para reconstruir lo mismo. Bernardo preguntó además si la burn-up se podía borrar por no usarla; se resolvió que no se toca — el pie numérico obligatorio de D-053 ya le da el veredicto que quiere ("cuánto atrasados") y la curva agrega CUÁNDO empezó el desvío a costo cero por estar ya construida. Se revisa tras un sprint de uso real. | Bernardo | Locked |
| D-070 | 2026-08-14 | Congelamiento del denominador del §12 con un evento nuevo confirmWeek y CERO maquinaria de fold nueva. Forma: Task ID = WEEK-<lunes ISO> (no vacío, estable, foldeable), Action = confirmWeek, Value = el lunes ISO validado con el mismo validador que ya usa pin, Note = el array JSON de los task ids congelados. Como el fold de D-009 es por (Task ID, Action) tomando el último por Timestamp, re-confirmar una semana REEMPLAZA el denominador sin escribir una línea de fold nueva, y todas las confirmaciones previas quedan en el log. El servidor impone un tope de largo sobre el Note y RECHAZA ruidosamente si se pasa: nunca truncar. | Se descartó la alternativa de reconstruir el denominador reproduciendo el log al instante de la confirmación, y el motivo es la razón por la que esta fila existe: una tarea de Rock que el motor propuso y que nadie tocó NO TIENE NINGÚN EVENTO. El log no la contiene, así que reproducir daría un denominador que ignora justo las tareas que salieron bien. Se descartó también una pestaña Weeks propia: agrega una tabla y un segundo mecanismo de escritura para algo que el log append-only ya resuelve. El tope con rechazo ruidoso es deliberado — un denominador truncado a la mitad no se ve roto, se ve como que la gente rindió el doble. | Bernardo | Locked |
| D-069 | 2026-08-14 | Reversión de las acciones destructivas: discard/undiscard y cancel/uncancel se implementan como PARES, espejando exactamente el par pin/unpin que ya existe — el miembro negativo pliega sobre el mismo slot del fold que el positivo, con Value vacío y Note opcional. No se agrega maquinaria nueva de fold. | Sin par de reversión, un clic errado durante el L10 (pantalla compartida, alguien marcando por otros, ritmo de reunión) sería permanente y solo corregible editando el Sheet a mano. El precedente de pin/unpin ya resolvió esta forma y repetirla cuesta casi nada, porque el validador del miembro negativo es el mismo. | Bernardo | Locked |
| D-068 | 2026-08-14 | Se puede CANCELAR una tarea de Rock desde la app. SUPERSEDE la regla del §11.4 que decía "Rock task: move to next week only. No discard." La cancelación es una acción DISTINTA del discard de una ad-hoc y lleva nombre distinto (cancel, no discard) porque es un acto distinto: tirar una ad-hoc no afecta a nadie, cancelar una de Rock cambia el plan del sprint y puede destrabar trabajo de otra persona. Reglas: (a) motivo obligatorio en el Note, igual que discard; (b) antes de confirmar se muestra la cascada de tareas afectadas calculada con cascadeOf (la misma de postergar, D-063d) — informativa, advierte y no bloquea; si no arrastra a nadie se cancela y listo, que es el caso que motivó la decisión; (c) una tarea cancelada sale del schedule en liveMode por el MISMO camino de código que una tarea deferred del plan (§4.6, D-017), así que sus dependientes quedan con la dependencia satisfecha. Es la primera modificación a engine.js desde la Fase 2 y por eso se especifica como reuso de un camino existente, no como concepto nuevo; (d) los work-days cancelados salen del denominador de la barra de progreso (§5.1) y se muestran al lado como total cancelado: no se esconden y no se dejan adentro, porque dejarlos adentro haría que el Rock nunca llegue a 100% y el semáforo quede rojo para siempre por trabajo que se decidió no hacer — el hábito que D-058 declaró el peor posible para un tablero nuevo; (e) la curva PLANIFICADA de la burn-up NO se recalcula al cancelar, sigue congelada per D-053, y el pie numérico nombra los días cancelados aparte. La barra de progreso contesta "cuánto del trabajo que queda está hecho", la burn-up contesta "cómo vamos contra lo que prometimos", y reescribir la promesa a mitad de sprint deja al gráfico incapaz de mostrar que se prometió de más; (f) NO se construye cancelación masiva de un milestone o un proyecto entero: si muere una línea completa, eso ya no es "una tarea dejó de tener sentido" sino que el Rock cambió, y eso se arregla regenerando sprint-plan.json, que es donde vive el plan. Un botón de cancelar-todo-esto convertiría al dashboard en editor del plan, que es justo lo que el §0 dice que no hace. Se revisa si pasa dos veces en un sprint real. | Bernardo lo planteó al revisar el §11.4: en la práctica una tarea de Rock puede dejar de tener sentido a mitad de sprint, y si el dashboard no deja cancelarla hay que arrastrarla de semana en semana hasta que termine el sprint — basura visible cada viernes en la única pantalla que se quiere que el equipo mire. Se evaluaron tres alcances: (A) todo en la Fase 8; (B) solo el evento y la desaparición de la vista, dejando progreso/burn-up para después; (C) no construirlo. Se eligió A. B se descartó porque dejaría el semáforo contando trabajo que ya se decidió no hacer, y un semáforo que miente es peor que un botón que falta. | Bernardo | Locked |
| D-067 | 2026-08-14 | Evento discard para tareas ad-hoc: se agrega a las acciones aceptadas por el backend, con Value vacío y Note OBLIGATORIA (el motivo), rechazada con código propio si viene vacía. El servidor rechaza además un discard cuyo Task ID no matchee ^T-\d{4}$: el §11.4 prohíbe descartar una tarea de Rock, y si esa regla vive solo en el frontend se saltea con una llamada directa al Web App, que no tiene restricción de origen. Es una excepción ESTRECHA a D-040 ("el Task ID no se valida contra el plan"): no se valida contra el plan, se valida contra el namespace, que el servidor sí conoce sin tener el JSON. Segunda mitad: discarded NO es un valor de setStatus. El §1 lo lista dentro del set de status de una ad-hoc, pero el §3 fija setStatus ∈ {open, in_progress, done} y el backend lo implementa así; se resuelve que discarded es un estado DERIVADO de la presencia de un evento discard en el fold. Una sola forma de descartar, no dos. Lo mismo vale para cancelled respecto de D-068. | Pregunta abierta del handoff de Fase 8. La regla del namespace apareció al notar que el servidor puede enforcear la asimetría del §11.4 sin conocer el plan, que era justamente la razón por la que D-040 había renunciado a validar el Task ID. | Bernardo | Locked |
| D-066 | 2026-08-14 | Creación de tareas ad-hoc: acción RPC nueva createTask, hermana de appendEvent y no un quinto evento — escribe en otra pestaña con otro esquema. Garantías del servidor: (a) id asignado por el servidor en el namespace T-NNNN, leyendo la última fila de Tasks e incrementando, bajo el mismo LockService que ya serializa las escrituras, así que dos personas creando a la vez en el L10 no colisionan; un id nunca se reusa, ni siquiera tras un discard; (b) week es OBLIGATORIO y el servidor escribe la fila de Tasks Y appendea el evento pin bajo el mismo lock, con lo cual "toda tarea nace con una semana" (§11.6) pasa a ser garantía del servidor y no convención del frontend; (c) validaciones: desc no vacío, owner exactamente uno y activo en People (reusando checkActor_ aplicado al owner además del actor) con rechazo NOMBRADO de "Both" por código propio, workDays numérico > 0 con fracciones, deadline ISO opcional, sourceIssueId opcional; (d) header guard posicional sobre la pestaña Tasks, mismo patrón de D-033; (e) write-then-verify releyendo las últimas filas de Tasks, igual que Events. sourceIssueId se acepta y se guarda desde ya aunque el §13 no exista: cuesta nada hoy y evita migrar la pestaña Tasks cuando llegue la Fase 9. | Pregunta abierta del handoff de Fase 8: el backend solo sabía appendear al Events log. El punto (b) se decidió sobre un riesgo concreto: con createTask y pin como dos llamadas separadas, un fallo de red entre ambas deja una tarea que existe y no está en ninguna semana — invisible en todas las vistas y sin forma de recuperarla, porque el §11.6 decidió a propósito que no hay backlog donde buscarla. No rompe la regla del §3 de que el Events log no se extiende estructuralmente: el pin es el pin de siempre. | Bernardo | Locked |
| D-065 | 2026-08-14 | Verificación de estado: cuando exista un hash de commit, verificar el contenido pidiendo el archivo POR HASH (raw.githubusercontent.com/F4LA/OperationsDashboard/<hash>/<ruta>) y no por la rama (/main/<ruta>). Un fetch de la rama devuelve caché vieja incluso con parámetro de query anti-caché; una URL por hash es única y nunca estuvo cacheada, así que refleja la verdad del commit. Solo si el hash confirma el contenido y aun así la rama difiere, se puede concluir propagación pendiente — nunca push fallido. | Pasó dos veces el mismo día. Primero con index.html: se leyó la rama, se concluyó que la Fase 5 había salido sin subir el parámetro de versión, y se hizo un commit de arreglo (471e8db) para un bug que ya estaba cerrado desde 06b568d — sin daño, pero trabajo inútil y una afirmación falsa registrada. Después con spec.md: la sesión PUSH reportó el commit 677d016 aplicado, el chat leyó la rama, vio v1.1 y 38515 bytes, y planteó que el push no había llegado; la lectura por hash devolvió 45818 bytes y v2.0, confirmando que el push estaba perfecto y la rama servía caché vieja. En ambos casos la sesión de Code tenía razón y la lectura del chat estaba equivocada por el mismo motivo. Complementa D-056: verificar contra el archivo, sí, pero contra el archivo del commit correcto. | Bernardo | Locked |
| D-064 | 2026-08-14 | Alcance preciso del pin, fijado al construir la Fase 7 y verificado en producción. (a) El pin gobierna EXCLUSIVAMENTE el bucket "Not started": no puede sacar una tarea de "Done this week" ni de "Working on", porque esos dos los gobierna el status real y un pin no puede des-terminar una tarea ni ocultarla mientras está en curso. El §6.3 solo habla de traer una tarea futura o posponer una, o sea el caso open/no-empezada. (b) Se confirma en uso real la consecuencia de D-063(c): posponer mueve la tarea fuera de la lista de esta semana pero NO cambia su fecha proyectada, así que la misma tarea puede seguir mostrándose con fecha de esta semana en el Sprint Board. Es el comportamiento decidido, no un bug; se revisa si molesta tras un sprint de uso real. (c) Una tarea owner:"Both" se muestra en las dos columnas per D-063(b), y postergarla desde cualquiera de las dos la posterga para ambos: es una tarea mostrada dos veces, no dos tareas. | Al implementar D-063(c) apareció que "el pin es un override de pertenencia al bucket" no decía SOBRE CUÁL bucket. Se resolvió en el momento y quedó verificado en el navegador antes de comitear; se registra ahora para que no se re-decida. Durante esa misma prueba salió un bug real que los tests no agarraron: la firma del repintado comparaba solo qué tareas había en cada bucket, así que pinear una que ya estaba ahí no repintaba su marcador ni su botón Release. Arreglado plegando el estado de pin de cada tarea en la firma. Precedente: probar en el navegador agarra lo que la suite no. | Bernardo | Locked |
| D-063 | 2026-08-14 | Contrato de la vista This Week (§6.3), fijado antes de construir. (a) BUCKETS: "Done this week" = tareas con status done y statusChangedAt truncado (D-028) dentro de la ventana; "Working on" = status in_progress, sin filtro de ventana; "Not started" = status open cuya proyección viva (§4.7) se solapa con la ventana, es decir plannedStart <= windowEnd Y plannedFinish >= windowStart. (b) Una tarea owner:"Both" aparece en la columna de CADA persona de owners[], no en una columna "Both" propia — la vista es de compromiso personal, y un compromiso conjunto lo es de los dos. (c) EL PIN NO MUEVE FECHAS: verificado que la firma del motor es liveMode(plan, currentState, todayISO) y no recibe pins, así que en v1 el pin es exclusivamente un override de PERTENENCIA AL BUCKET — trae una tarea futura a la lista de esta semana o la saca, sin tocar la proyección ni el chip on-track ni la burn-up. "Postergar" = pin al lunes de la semana siguiente; "traer" = pin al lunes de esta semana; "soltar" = unpin y vuelve a la proyección automática. (d) La cascada downstream que el §6.3 exige mostrar antes de postergar se calcula con el fan-out de dependencias de validate.js (D-024), se presenta como lista de tareas afectadas con su dueño, y es informativa: advierte, no bloquea. (e) El dropdown de "+ add to this week" lista solo tareas open cuyas dependsOn y crossDependsOn estén TODAS en done según currentState; una tarea bloqueada no aparece. | El §6.3 describe la vista en prosa pero deja sin definir el criterio exacto de cada bucket, el tratamiento de "Both", y sobre todo si el pin es un override de presentación o una entrada del motor. Lo último importa: si el pin moviera fechas, el motor dejaría de ser la única fuente de la proyección y el chip on-track podría falsearse a mano, que es justo lo que D-023 decidió no permitir. Se elige el override de presentación: la persona declara su compromiso, el motor sigue diciendo la verdad. Revisable si un sprint real pide que el pin reproyecte. | Bernardo | Locked |
| D-062 | 2026-08-14 | Navegación entre vistas: Opción B — control segmentado ("Sprint board" / "This week") en la topbar, a la izquierda del selector de persona. Elegido viendo tres mockups con datos reales de Rock 3 (mismo patrón de D-048 y D-051). La vista activa se persiste en localStorage con la clave `opsdash.view` (misma convención que `opsdash.actor`, §3), default "board" si no hay valor o el valor no es válido. Consecuencia aceptada a propósito: cuando entre la Fase 6 el control queda con tres segmentos y probablemente haya que migrar a tabs — se acepta ahora y se revisa entonces, en vez de construir tabs para una tercera vista que todavía no existe. | Quedó sin resolver al planear la Fase 6 y aterrizó en la Fase 7 porque This Week es la primera segunda pantalla real. Bernardo no tenía preferencia en abstracto; eligió sobre mockups. B gana por no consumir altura vertical permanente en una pantalla que ya carga topbar, summary bar y burn-up antes del primer Rock. | Bernardo | Locked |
| D-061 | 2026-08-14 | Semana de operaciones y clave canónica de semana, escritas por primera vez. La ventana se calcula desde `opsWeekStartDay` de la Settings (valor real verificado en el Sheet: la cadena "Friday", nombre de día en inglés): windowStart = el opsWeekStartDay más reciente en o antes de hoy; windowEnd = windowStart + 6 días CALENDARIO (jueves). La CLAVE de la semana — el único valor que viaja en un evento pin — es el lunes ISO contenido en esa ventana (con inicio viernes, windowStart + 3). Fundamento de por qué el lunes es una clave válida aunque no sea el inicio de la semana: cualquier ventana de 7 días contiene exactamente un lunes, así que la clave es única y sigue siéndolo si algún día cambia opsWeekStartDay. Parseo: nombre de día en inglés (case-insensitive) → índice; si el valor falta o no parsea, fallback a Friday, documentado en el call site y nunca silencioso (mismo patrón que el fallback de band en app.js línea 130). | El §3 del spec YA fija que el Value de un pin es el lunes ISO de la semana, y backend/Code.gs (líneas 329-345) solo implementa eso, rechazando con BAD_VALUE_PIN cualquier otro día. Pero el §6.3 corre la semana de viernes a jueves y en ningún lado estaba escrito el mapeo entre una y otra, ni cómo se computan los límites desde la Settings. Sin esta fila, el primer pin del frontend habría sido rechazado por el servidor o —peor— habría mandado un lunes de la semana equivocada. Además queda registrado que opsWeekStartDay se leía en app.js y se descartaba: hasta hoy ningún código lo consumía. | Bernardo | Locked |
| D-060 | 2026-08-14 | Las dependencias no cruzan Rocks: se mezclan dentro del mismo Rock, no entre Rocks (regla vigente, revisable). `crossDependsOn` sigue soportado por el validador y el motor, pero queda sin ejercitar. Consecuencia para la Fase 6: los "conectores cross-Rock" que pide el §6 no van a tener nada que dibujar, así que ese requisito queda en suspenso, no cancelado. Lo que SÍ cruza Rocks es la capacidad de las personas: verificado contra engine.js (líneas 338-339) que availableFrom es un único mapa por persona para todo el sprint, no uno por Rock, así que una persona con tareas en dos Rocks se agenda de a una a la vez sin cambios en el motor. | Al planear la Fase 6 se asumió que con 4 Rocks habría dependencias cruzadas. Bernardo confirmó que no: el entrelazado ocurre dentro de cada Rock. Registrarlo evita construir un conector que nunca se dibuja y deja explícito el único acoplamiento real entre Rocks, que es de recurso y ya funciona. | Bernardo | Locked |
| D-059 | 2026-08-14 | Se invierte el orden del §9: la Fase 7 ("This Week" + pin manual) se construye ANTES que la Fase 6 (Network/Timeline). Razón: los dos rituales reales de la empresa — el L10 del viernes y el chequeo de Bernardo del miércoles — apuntan a This Week, que el propio §6.3 llama el backbone del L10 reestructurado; ningún ritual pide un Gantt. Además la Fase 6 queda con una PRECONDICIÓN explícita: hoy el dashboard tiene cargado 1 Rock de los 4 que corre el sprint, y una timeline dibujada sobre un cuarto del sprint no muestra lo único que la justifica (cuál Rock arrastra a cuál). Orden resultante: Fase 7 → carga de los 4 Rocks → Fase 6. La Fase 6 no se cancela, se difiere hasta tener con qué. | Bernardo frenó a preguntar si la timeline realmente sumaba. En el análisis, el chat afirmó erróneamente que el sprint tenía un solo Rock — leyó el sprint-plan.json (que contiene uno) y concluyó sobre la realidad de la empresa (que corre cuatro). Bernardo corrigió. La corrección devuelve valor a la Fase 6 pero no cambia el orden, porque con 4 Rocks el chequeo del miércoles se vuelve más necesario, no menos. Precedente: trazar un archivo no autoriza a afirmar sobre el mundo. | Bernardo | Locked |
| D-058 | 2026-08-14 | Arranque del tracking en S3-2026: se elige Opción A — backfill. El equipo carga en el board las tareas ya completadas desde el 2026-07-27 en vez de esperar a S4 con el semáforo en rojo permanente. Consecuencia aceptada a propósito: el Timestamp es server-side (D-041), así que TODO lo retroactivo queda fechado 2026-08-14 y la curva "actual" de la burn-up queda plana desde el inicio del sprint y da un salto vertical ese día. No es un defecto de §5.2 — el dashboard efectivamente no sabía nada antes de esa fecha. Se explica en voz alta en el L10 ("la curva salta hoy porque hoy cargamos el historial") en vez de disimularlo. Cada persona marca SUS propias tareas: el Events log guarda el actor, y un backfill hecho por una sola persona falsearía la autoría. | El board nació en la mitad de un sprint que venía corriendo en Asana desde el 27 de julio, así que al abrirse mostraba 0% y "Projected finish 2026-10-05" — exactamente 15 días laborales después del 2026-09-14 del plan, que son los 15 días laborales transcurridos del sprint con cero marcas. El motor decía la verdad sobre lo que sabía; lo que faltaba era el historial. Un rojo sin explicar enseña al equipo a ignorar el semáforo, que es el peor hábito posible para un tablero nuevo. | Bernardo | Locked |
| D-057 | 2026-08-14 | Regla de la casa: todo commit que toque un archivo .js o styles.css sube el parámetro de versión de TODOS los assets en index.html (?v=YYYYMMDD<letra>). Se incluye el <link> de styles.css, que hasta la Fase 5 no llevaba parámetro y ahora sí. | Descubierto al cerrar Fase 5: el commit 45f8afd agregó el div del panel de burn-up a index.html sin subir el ?v=, así que cualquier navegador con caché de Fase 4 hubiera recibido el HTML nuevo con board.js/metrics.js viejos — panel vacío y cero repintado. La sesión BUILD extendió la regla al CSS por su cuenta y con razón: .burnup-row{height:34px} vive en el CSS nuevo, así que un styles.css cacheado colapsa ambas gráficas a altura cero, mismo síntoma por otra puerta. | Bernardo / Claude Code | Locked |
| D-056 | 2026-08-14 | CORRIGE EL REGISTRO DE D-052: esa fila afirma que BUILD ajustó el contraste del flag de overshoot a ≥4.5:1 AA, pero ese cambio de color nunca llegó a commitearse — de aquel pase solo quedó el bump de tamaño del flag de 18px a 24px. El flag siguió en 4.41:1 hasta hoy. Corregido ahora en el commit 06b568d: --red pasa de #C13B3B a #B83434, que da 4.87:1 sobre --red-bg y 5.87:1 sobre blanco. Verificados los nueve consumidores de --red: tres pasan de 4.41 a 4.87, ninguno baja, y toast-error (blanco SOBRE --red) también mejora porque oscurecer el fondo sube el contraste contra texto blanco. D-052 queda Locked pero parcialmente inexacta; esta fila es el registro correcto. | La sesión BUILD lo detectó revisando el historial de styles.css al arrancar Fase 5, y el chat de diseño lo confirmó calculando el ratio real. Una fila del log que da por hecho algo que no se commiteó es peor que no tenerla: hace confiar en una verificación que nunca ocurrió. Vale como precedente — un cierre de fase se verifica contra el archivo, no contra el reporte. | Bernardo / Claude Code | Locked |
| D-055 | 2026-08-14 | Fase 5 (Métricas, §5) CERRADA y probada en el sitio desplegado (f4la.github.io), estándar D-049. Commits 45f8afd (las tres partes) + 06b568d (arreglos de cierre). Entregado: (A) OpsDashMetrics.burnupSeries — un punto por día calendario, planned desde el plan congelado, actual desde statusChangedAt truncado y null después de hoy, per D-053; (B) gráfica SVG inline en dos scopes (sprint-wide bajo la summary bar, por Rock dentro del header) con línea punteada/sólida, marca de hoy, pie numérico y role="img"; (C) diffAndRepaint por firma de tarea/milestone/Rock, per D-054. Regresión 181/181 (157 previos + 24 de tests/burnup.test.js). engine.js, validate.js y events.js sin tocar. Dos mejoras que BUILD aportó sobre la especificación y quedan adoptadas: el axisEnd se calcula POR SCOPE (el desborde de un Rock no estira el eje de los demás) y la protección de filas se dispara solo con input enfocado o con texto tipeado (la primera versión hubiera protegido todas las filas siempre, porque toda tarea sin deliverable muestra un input vacío). | Evidencia del E2E en producción: con cinco tareas marcadas, el milestone M2 pasó de 2026-09-21 a 2026-09-18, M3 de 2026-09-25 a 2026-09-22, y la tarea "Lock the refund rate target %" (owner Both, nunca tocada, sigue en Open) se movió sola de 2026-09-21 a 2026-09-18 — la reproyección cross-dependencia visible en pantalla, que es exactamente el defecto que D-054 arregla. Rastro de prueba borrado a mano del tab Events. | Bernardo | Locked |
| D-054 | 2026-08-13 | Repintado tras marcar: se corrige un defecto encontrado leyendo el código en el chat de diseño de Fase 5 — recompute() sí recalcula liveMode en cada marca (§4.7), pero solo se repintaban la fila marcada, el header de su Rock y la summary bar, dejando toda fila de tarea y header de milestone downstream con su plannedFinish viejo hasta un Refresh. Solución elegida: diff dirigido (snapshot del liveResult previo, comparar firmas por tarea/milestone/Rock, patchear solo lo cambiado) en vez de un render() completo, porque el re-render remonta el <select> que la persona acaba de usar y le quita el foco. Guarda explícita: no patchear una fila con input de deliverable abierto o con foco. | La reproyección viva es la razón de ser del dashboard frente a Asana; recalcularla sin mostrarla la vuelve invisible. No estaba registrado como bug en ningún lado — apareció al trazar el código de Fase 4 contra §4.7 antes de escribir el prompt de Fase 5. | Bernardo | Locked |
| D-053 | 2026-08-13 | Burn-up (§5.2/§5.3) definida antes de construir: gráfica SVG inline chica, sin librería ni canvas, sin hover ni tooltips (board interno de 5 personas), con pie numérico obligatorio en texto plano ("Actual X of Y planned work-days — Z behind") que además cubre por sí solo la lectura del indicador. Matemática en metrics.js como función pura burnupSeries(frozenTasks, currentState, taskIds, axisStart, axisEnd, today) → un punto por día CALENDARIO; planned(D) desde el plan congelado (nunca recalcular planMode); actual(D) desde statusChangedAt truncado (D-028) y null para toda fecha > today (no se dibuja futuro); total = Σ workDays no-deferred del scope = 40.75 para el sprint actual. Dibujo en board.js (markup es su dominio, matemática es de metrics.js). Eje X de sprint.start a max(sprint.end, plannedFinish congelado más tardío) = 2026-09-14 por D-023: cortar en sprint.end dejaría la curva planificada sin llegar al techo y se leería como un bug. Se renderiza en DOS scopes, ambos siempre visibles: sprint-wide bajo la summary bar y por Rock bajo la fila de métricas del header. | El chip on-track ya resuelve el veredicto de §5.2 con los dos escalares en "hoy"; §5.3 lista la gráfica como opcional, así que el tamaño del esfuerzo era una decisión abierta. Se elige el punto medio: la curva agrega CUÁNDO empezó la desviación, que los números solos no dan, sin el costo de un chart interactivo. Duplicar la gráfica en Rock y sprint es redundante hoy (Rock 3 es el único Rock del sprint) y se acepta a propósito: el componente se escribe una vez y en un sprint de 2-3 Rocks es lo que permite ver cuál arrastra. | Bernardo | Locked |
| D-052 | 2026-08-13 | Rediseño visual del Sprint Board (D-051) CERRADO. BUILD ajustó contraste del flag de overshoot a ≥4.5:1 AA y agregó separación visual entre Project/Milestone (margin+padding en .project-divider/.milestone-group, además de la corrección de aire pedida tras revisión visual de Bernardo). Confirmado E2E en navegador real (f4la.github.io): dropdown de status escribe y verifica contra el Sheet real (M1-t1, in_progress→open, actor Bernardo, filas de prueba borradas a mano). Regresión 157/157 intacta. | Segunda vuelta de feedback visual de Bernardo tras ver la primera versión del rail — pidió más separación entre secciones; BUILD lo resolvió y lo confirmó contra el plan real antes de este cierre, mismo estándar que D-049 exigió para Fase 4. | Bernardo | Locked |
| D-051 | 2026-08-13 | Rediseño visual del Sprint Board decidido: Option B (rail izquierdo coloreado por nivel — navy 6px project, steel 6px milestone, border 6px task — con indentación creciente 16/26/44px) para la jerarquía Rock→Project→Milestone→Task. El control de status pasa de ciclo de 3 clics a <select> dropdown, mismo write path (postEvent("setStatus",...) + verify) intacto. La alerta de overshoot (D-023) se resuelve con tooltip custom en hover/focus (reemplaza el title nativo del navegador, poco visible) MÁS una leyenda estática siempre visible en la summary bar ("⚠ = projected past sprint end"), para que no dependa de encontrar el hover. Cero cambios de lógica — solo board.js + styles.css. BUILD corre web-design-guidelines contra ambos archivos antes de comitear. | Bernardo vio 3 mockups reales (A/B/C) generados con datos reales de Rock 3 en el chat de diseño y eligió B por decidir mejor viendo que en abstracto — mismo patrón de decisión que D-048. | Bernardo | Locked |
| D-050 | 2026-08-13 | Pase de rediseño visual del Sprint Board ANTES de Fase 5: se abre un chat de diseño dedicado porque la UI de Fase 4, aunque funcional, quedó plana (proyectos apenas distinguibles del texto de milestone, filas apeñuscadas, alerta ⚠ de overshoot sin explicación visible). Se rediseña ahora, no después, porque Fases 5/6/7 reúsan los mismos patrones visuales y heredarían el problema. Decisión ya tomada dentro del pase: el control de status pasa de ciclo de 3 clics (D-nada, era comportamiento aprobado en el reporte de Fase 4) a un dropdown de selección directa de estado. BUILD aplicará el skill web-design-guidelines y revisará strong-standard-web-audit al implementar. | Feedback de Bernardo al ver el board en vivo tras el E2E. El ciclo de 3 clics para volver a open era poco intuitivo; la jerarquía visual no comunicaba estructura. | Bernardo | Locked |
| D-049 | 2026-08-13 | Fase 4 (Sprint Board, Views 1+3) probada end-to-end en el navegador real desplegado (f4la.github.io/OperationsDashboard), no solo con mock: seleccionar actor, ciclar status de una tarea real (M1-t1) open→in_progress→done→open, y confirmar que verifyEvent se autoconfirma sin errores de consola — la mitad del write-then-verify que no se pudo probar desde Node por la restricción de referrer (D-043). Las filas de prueba (SMOKE-F4-t1 + las 3 de M1-t1) se borraron a mano del Sheet. Fase 4 QUEDA CERRADA Y PROBADA. | Cierre del E2E que quedó pendiente al final del reporte de BUILD (commit 1acc181). El POST real ya estaba probado; faltaba la autoconfirmación en su destino real. | Bernardo | Locked |
| D-048 | 2026-08-13 | Fase 4 — patrón visual: CoachPulse es la referencia de estética (tipografía, color, espaciado); la sesión BUILD lo inspecciona en disco antes de escribir styles.css, no lo copia literal. UI en inglés per §10. | Pregunta abierta #4 de Fase 4. | Bernardo / Claude Code | Locked |
| D-047 | 2026-08-13 | postEvent/verifyEvent (write path real del §3 traducido a cliente) viven en dashboard/events.js, no en un módulo nuevo — mismo dominio que el fold ya asignado ahí por D-036. Verify reintenta con backoff 0/500/1000/2000ms sobre las últimas 40 filas de Events antes de reportar VERIFY_TIMEOUT. | Pregunta abierta #2 de Fase 4; el spec §3 describe la mecánica pero no dónde vive el código cliente. | Bernardo / Claude Code | Locked |
| D-046 | 2026-08-13 | El frontend emite únicamente la forma shortcut del payload ({action, sprintId, taskId, value, actor, note}), nunca el envelope appendEvent. El backend sigue aceptando ambas (D-039) pero el board fija una sola forma como su uso canónico. | D-039 dejaba abiertas dos formas equivalentes; Fase 4 necesita fijar cuál emite el cliente real. | Bernardo / Claude Code | Locked |
| D-045 | 2026-08-13 | Confirmado: es seguro commitear en dashboard/config.js tanto la API key (restringida por referrer, D-035) como la Web App URL sin restricción de origen — una URL de cliente-navegador nunca es secreta (visible en devtools) con o sin commit, así que commitearla no agrega exposición real sobre la ya aceptada en §3. | Pregunta abierta #1 de Fase 4, reconfirmando la nota de seguridad del §3 antes de escribir el archivo. | Bernardo / Claude Code | Locked |
| D-044 | 2026-08-13 | Falso positivo corregido en el harness de D-037: la verificación "nada se escribió" devolvía PASS cuando la LECTURA fallaba (se ponía verde por quedarse ciega). Ahora una lectura fallida es inconclusive → se reporta como fallo, nunca como pass. Se agregó además un preflight que confirma lectura ANTES de escribir, para cortar en segundos ante un problema de credenciales en vez de escribir filas no verificables. | Descubierto al correr el smoke test real por primera vez. Un test que aprueba cuando no puede ver es peor que uno que falla; la regla vale para todo harness futuro del proyecto. | Bernardo / Claude Code | Locked |
| D-043 | 2026-08-13 | Tensión de diseño registrada: la restricción por referrer de la API key (§3) la vuelve inusable desde cualquier cliente que no sea navegador, y el harness de D-037 (Node) necesita leer con esa key. Resolución: el test manda el origen de GitHub Pages como header Referer; la key sigue restringida. No evade la restricción (un Referer es client-supplied en cualquier plataforma; la restricción nunca fue una barrera de auth, solo evita que una key copiada sirva en otra página). Es el primer choque del spec consigo mismo entre §3-lectura y §3-restricción. | Apareció al correr el smoke test: la key bien configurada para producción rechazaba al test con API_KEY_HTTP_REFERRER_BLOCKED. | Bernardo / Claude Code | Locked |
| D-042 | 2026-08-13 | setDeliverable acepta solo URLs http/https, tope 2000 chars; un javascript: u otros esquemas se rechazan con BAD_VALUE_URL. El valor se renderiza como link clickeable en el board (§6), así que un esquema peligroso ahí sería XSS almacenado. | §3 pide "URL bien formada" sin fijar esquemas permitidos; la decisión cierra el hueco de seguridad antes de que el board consuma el valor en Fase 4/5. | Bernardo / Claude Code | Locked |
| D-041 | 2026-08-13 | Robustez del Timestamp server-side: se escribe el ISO, se fuerza la celda de la columna Timestamp a formato texto (@) y se reescribe, porque Sheets auto-convierte un ISO a valor-fecha y la API lo devolvería como string con formato de locale, rompiendo el fold en silencio. El Timestamp lleva offset real (ej. -05:00), no hora local etiquetada como Z; por eso events.js ordena numéricamente (por instante), no por string —  un cambio de DST dentro del sprint movería el offset. | §3 pide timestamp server-side en timezone del Sheet pero no anticipa la auto-conversión de Sheets ni el efecto de DST sobre el ordenamiento del fold. | Bernardo / Claude Code | Locked |
| D-040 | 2026-08-13 | Columna Active en People: un Actor marcado inactivo se rechaza con ACTOR_INACTIVE (distinto de ACTOR_UNKNOWN); pero una celda Active vacía se trata como activo, para que un People a medio llenar no deje a todo el equipo afuera. El Task ID NO se valida contra el plan (§3 solo pide rechazar vacío, y el Apps Script no tiene el JSON). Todas las respuestas son HTTP 200 incluso los errores (ContentService no puede setear status); el frontend debe ramificar por el campo `ok`, nunca por el status HTTP. | §3 nombra la columna Active pero no fija su semántica; sin esto la columna no serviría. El resto son contratos server↔frontend que el §3 no explicita y la Fase 4 necesita conocer. | Bernardo / Claude Code | Locked |
| D-039 | 2026-08-13 | Forma del payload del write path (CONTRATO con el frontend de la Fase 4): el doPost acepta DOS formas equivalentes — el atajo `{action:"setStatus"|"setDeliverable"|"pin"|"unpin", ...}` y el envelope `{action:"appendEvent", eventAction:"setStatus"|..., ...}`. No hay ambigüedad porque "appendEvent" y las 4 acciones son conjuntos disjuntos. Se acepta la doble forma en vez de forzar una sola: el costo hoy es nulo y evita adivinar mal contra el §3, que nombra el RPC "appendEvent" pero también llama "Action" a la columna. Revisable en Fase 4 si conviene converger a una. | §3 usa "appendEvent" como nombre del RPC y "Action" como columna del Sheet, sin fijar la forma exacta del cuerpo POST. Aceptar ambas cierra la fase sin bloquear el frontend. | Bernardo / Claude Code | Locked |
| D-038 | 2026-08-13 | Apps Script expone solo `doPost`; no hay `doGet` ni endpoint de lectura propio. Toda lectura (People, fold de Events) va directo del frontend a Sheets API v4, tal como ya lo fija el diagrama de arquitectura del §3. | Pregunta abierta #4 de Fase 3: cómo se traduce el fold de Events al currentState de D-027. El spec ya lo resolvía; solo faltaba decirlo explícito antes de escribir código. | Bernardo / Claude Code | Locked |
| D-037 | 2026-08-13 | Harness de prueba para Fase 3: `tests/appsscript-smoke.test.js`, script Node plano (sin framework, misma convención de D-022) que pega directo al Web App desplegado — casos felices con write-then-verify real vía Sheets API v4, y cada rechazo server-side. Header-guard y concurrencia real quedan como procedimiento manual documentado en el propio archivo, no automatizados. | Pregunta abierta #3: cómo probar el backend sin frontend real todavía. | Bernardo / Claude Code | Locked |
| D-036 | 2026-08-13 | El fold de Events (D-009: último evento por (Task ID, Action) por Timestamp) se implementa en `dashboard/events.js` durante la Fase 3 — el módulo stub deja de ser stub — produciendo exactamente el shape `currentState` que D-027 fijó para liveMode. No se toca engine.js. | Pregunta abierta #4, mitad de "dónde vive el código", complementaria a D-038. | Bernardo / Claude Code | Locked |
| D-035 | 2026-08-13 | El código de Apps Script se commitea en el repo en `backend/Code.gs` (archivo único para v1). La URL del Web App desplegado NO se commitea en esta fase (no hay frontend todavía que la necesite); se lee desde variable de entorno en el test script. Se commiteará en `dashboard/config.js` en la Fase 4, cuando el Sprint Board la consuma. | No estaba fijado dónde vive el backend en el repo — necesario antes de escribir el .gs. | Bernardo / Claude Code | Locked |
| D-034 | 2026-08-13 | Event ID server-side con formato `E-<epochMillis>-<4 chars base36 random>`, generado dentro de doPost bajo LockService. Se prefiere sobre un contador monotónico porque no requiere leer/mantener estado adicional; LockService ya serializa la escritura. | Pregunta abierta #2 de Fase 3. | Bernardo / Claude Code | Locked |
| D-033 | 2026-08-13 | Esquema de columnas fijado: People = `Name \| Slack/Email \| Active` (Active boolean); Events = `Event ID \| Sprint ID \| Task ID \| Action \| Value \| Actor \| Timestamp \| Note` (Timestamp ISO completo, sin truncar — D-028 trunca solo al consumir en live mode); Settings = exactamente dos filas para v1, `onTrackBandWorkDays` y `opsWeekStartDay`, formato `Key \| Value \| Notes`. | Pregunta abierta #1 de Fase 3; el spec §3 daba los nombres pero no el detalle de implementación. | Bernardo / Claude Code | Locked |
| D-032 | 2026-08-13 | Una tarea in_progress sin statusChangedAt devuelve el error MISSING_STATUS_CHANGED_AT en vez de crashear — mismo tratamiento que MISSING_COMPLETED_AT para done. | D-027–D-031 solo definieron el caso done sin timestamp. El caso in_progress sin timestamp no debería ocurrir en la práctica (el propio evento setStatus=in_progress es lo que genera el timestamp), pero el manejo simétrico de errores evita un crash silencioso si algún día pasa. No ejercitado por ningún fixture. | Claude Code (implementación) / Bernardo (revisión) | Locked |
| D-031 | 2026-08-13 | El algoritmo de live mode (§4.7) corre en 3 pasadas sobre el mismo grafo/comparador de plan mode: (1) fijar tareas done a su fecha real de finalización; (2) fijar tareas in_progress a su inicio real, proyectando el fin con clamp a "hoy" si cae en el pasado; (3) agendar las open restantes con el loop exacto de §4.5/D-021, agregando today como tercer piso en startPos = max(depFinish, resourceFree, today). El rollup (§4.6) y el red-flag no cambian. | §4.7 solo dice la idea general ("fijar lo done, re-correr 4.5 desde hoy"); no define cómo conviven las tres categorías de status ni cómo se integra "hoy" al loop existente sin duplicar su lógica. | Bernardo / Claude Code | Locked |
| D-030 | 2026-08-13 | Seeding de availableFrom para una tarea done: el dueño se liberó del trabajo activo en completedAt − waitDays (calendario), no en completedAt. Asume que la espera real coincidió con la planeada. | El Events log (D-009) no timestampea por separado "trabajo terminado" vs. "ahora esperando"; solo hay un evento setStatus=done. Sin esta aproximación, una tarea con wait largo dejaría al dueño "ocupado" de más y correría en falso las siguientes tareas de esa persona. Marcada como aproximación práctica, no como certeza — revisar si algún sprint real la ejercita de forma contraintuitiva. | Bernardo / Claude Code | Locked |
| D-029 | 2026-08-13 | Una tarea in_progress fija su inicio en el statusChangedAt real (cuándo se marcó in_progress) y proyecta el fin con workDays/waitDays normales; si ese fin proyectado cae antes de "hoy", se clampea a hoy — una tarea que sigue abierta no puede haber terminado en el pasado. La tarea sale del loop de prioridad §4.4 (no compite por orden, ya está fija por la realidad, D-008). | §4.7 solo dice cómo tratar las done; no dice nada de in_progress. Sin el clamp, una tarea atrasada reportaría una fecha de fin ya pasada, lo cual no tiene sentido para algo que sigue sin terminar. | Bernardo / Claude Code | Locked |
| D-028 | 2026-08-13 | Los timestamps que consume live mode se truncan a su fecha calendario (YYYY-MM-DD); el motor trabaja a granularidad de día, igual que el eje de plan mode (D-020), que no tiene noción de hora del día. | El Events log guarda timestamp completo (D-009), pero el eje continuo del motor no tiene una unidad más fina que el día laboral; introducir hora-del-día complicaría D-020 sin que el dashboard (vista semanal) lo necesite. | Bernardo / Claude Code | Locked |
| D-027 | 2026-08-13 | Contrato de entrada de live mode, fijado antes de que exista la Fase 3: `liveMode(plan, currentState, todayISO)`, donde `currentState[taskId] = { status: 'open'\|'in_progress'\|'done', statusChangedAt: ISO \| null }` — exactamente la forma que produce el fold de Events por (taskId, "setStatus"), último por timestamp (D-009). `today` es un parámetro explícito, nunca `Date.now()` dentro del motor (testeable de forma determinista). | Sin fijar esto ahora, la Fase 3 (Sheet + Apps Script) podría terminar produciendo un shape distinto al que live mode espera, forzando un adaptador o un rediseño. | Bernardo / Claude Code | Locked |
| D-026 | 2026-08-13 | Una tarea degenerada con workDays:0 Y waitDays:0 (no existe en Rock 3) se clampea para terminar en su propio día de inicio, en vez de reportar el día anterior por la convención de posición-entera del eje continuo (D-020). | §7 contempla la tarea de pura espera (workDays:0, waitDays>0) pero no el caso doble-cero. Sin el clamp, el mapeo posición→fecha reportaría un finish un día antes del start. | Bernardo / Claude Code | Locked |
| D-025 | 2026-08-13 | Dos interpretaciones que Rock 3 NO ejercita, registradas como provisionales: (a) executionOrder (§4.4) es por-persona y solo ordena tareas con la misma firma de dueño — pares de dueños distintos caen a la prioridad §4.4; (b) owner:"Both" ocupa a TODAS las personas de `people` (en Rock 3, people == owners del rock == 2, así que es exacto). Confirmar ambas antes del primer uso real: uso de executionOrder (p.ej. la palanca de compresión de D-023), o un sprint con más participantes que los owners de un Rock (donde "Both" podría querer decir "los owners del Rock", no "todos"). | §4.4 no define la interacción de executionOrder con tareas "Both" ni entre personas; §4.3 dice "Both consume ambos owners" sin distinguir owners-del-Rock vs participantes-del-sprint. Con Rock 3 no hay diferencia observable; se registra la lectura actual sin bloquear, para revisarla con datos que la ejerciten. | Bernardo / Claude Code | Provisional — confirmar antes del primer uso |
| D-024 | 2026-08-13 | engine.js (OpsDashEngine) reutiliza OpsDashValidate para el índice de ids y el fan-out de dependencias del §4.2, en vez de duplicar esa lógica (ya probada en Fase 1); la resuelve desde window con fallback a globalThis (para Node). Implica orden de carga fijo en index.html (validate antes que engine), que ya es la convención de la casa. | §4 no dice de dónde sale la resolución de ids; duplicarla arriesga que dos copias se desincronicen (la cicatriz D-088 del testimonial-dashboard). Reusar la única fuente probada es más seguro. | Bernardo / Claude Code | Locked |
| D-023 | 2026-08-13 | En plan mode, Rock 3 cierra el 2026-09-14 (M4, M7, M20, M23 caen en goLive, un día pasado sprint.end 2026-09-13), así que el dashboard lo marcará en rojo desde que carga el plan. Se decide NO ajustar el motor ni la data para forzar el cierre en fecha: el motor dice la verdad. Si en el futuro se quiere comprimir, la palanca prevista es executionOrder (§4.4), no tocar el engine. | El objetivo del on-track es ser el indicador líder honesto que Asana no da; "arreglar" el rojo falseando duraciones u orden derrotaría el propósito. El overshoot es de 1 día y depende del desempate, no es catastrófico. | Bernardo | Locked |
| D-022 | 2026-08-13 | El harness de pruebas del motor vive en /tests: un script Node plano sin framework (tests/engine.test.js) que carga sprint-plan.json, corre el engine en plan mode y compara contra tests/expected-plan-mode.json (fixture congelado, 46 tareas activas, generado en esta sesión de diseño). El fixture es el árbitro de la semántica de fechas; si el engine y el fixture discrepan, se resuelve en el chat de diseño, nunca cambiando uno de los dos en silencio. | La Fase 1 dejó su harness sin commitear (fuera de scope entonces). El motor es la pieza de mayor riesgo; necesita red de regresión permanente. "Sin framework" mantiene la convención "no build step" del resto de los dashboards. | Bernardo | Locked |
| D-021 | 2026-08-13 | El algoritmo de scheduling es determinista y se fija explícitamente por encima de la redacción "por lotes" del §4.5: se recomputa el conjunto ready y se re-ordena globalmente por prioridad (§4.4: a=desbloquea más downstream, b=hardDeadline más temprano, c=mayor duración total) DESPUÉS de agendar cada tarea, agendando una tarea por iteración. Desempate final y total: task.id ascendente. | §4.5 está redactado como si agendara todas las ready de una pasada; con fracciones y readiness que cambia dentro del lote, dos implementaciones podrían diferir. Recomputar tras cada tarea + desempate por id garantiza que el resultado sea único y reproducible (condición para que el fixture de D-022 tenga sentido). | Bernardo | Locked |
| D-020 | 2026-08-13 | El motor usa un eje continuo de días laborales para las duraciones fraccionarias (cada día laboral = ancho 1.0; fines de semana = ancho 0), de modo que start + workDays es suma real sin lógica especial de findes, y dos tareas de medio día del mismo dueño pueden caer el mismo día calendario (cumpliendo §4.1). Semántica de fin: (a) tarea con waitDays=0 → su fin queda en posición continua (puede ser mitad de día) y un dependiente del mismo dueño puede seguir ese mismo día; (b) tarea con waitDays>0 → el fin se ancla al inicio del día laboral en/tras (fin-de-trabajo + waitDays días calendario); (c) availableFrom del dueño se libera en la posición de fin del trabajo activo, libre DURANTE la espera (§4.3/§4.5). | §4.1 dice que 0.5 workDay ocupa medio día laboral, pero §4.5 no explicita cómo conviven fracciones ni cómo se ancla el fin cuando hay espera calendario. Sin fijarlo, tareas fraccionarias encadenadas (p.ej. M6-t1→M6-t3, ambas Bernardo 0.25d) se calcularían mal. El eje continuo es la lectura correcta de §4.1 y simplifica la aritmética. | Bernardo | Locked |
| D-019 | 2026-08-13 | La validación (§7) distingue errores (bloquean: ok=false) de warnings (no bloquean). Errores: todo lo de §2/§7, más sprint.start/end no-ISO, end antes de start, y people que contenga "Both" (reservado para owner conjunto). Warnings: status presente en el JSON (§2 dice ignorarlo), schemaVersion desconocida, desc vacía, milestone sin tasks, y dependencia a un milestone vacío (se satisface vacuamente). Cada error/warning nombra el id y su ruta. | §7 solo pide "un error claro". La taxonomía permite cargar el plan mostrando todos los problemas juntos en vez de abortar al primero, y separa lo que frena la carga de lo que solo merece aviso. | Bernardo | Locked |
| D-018 | 2026-08-13 | La detección de ciclos corre sobre dos grafos por separado: (1) el grafo de scheduling de tasks de §4.2, y (2) el grafo declarativo de dependsOn entre milestones. Las aristas de milestone NO se agregan al grafo de scheduling, para no alterar la semántica del motor que §4.2 define estrictamente. | §4.2 solo define el grafo de tasks; los milestones también traen dependsOn (redundante con sus tasks en Rock 3). Revisarlos aparte detecta un ciclo declarativo sin contaminar el scheduling. Las tasks deferred se incluyen en la detección porque un ciclo latente reaparece al activarlas. | Bernardo / Claude Code | Locked |
| D-017 | 2026-08-13 | El deferred:true a nivel milestone se propaga a todas sus tasks: se excluyen del scheduling, del denominador de progreso (§5.1) y de la proyección, y se muestran en estado muted. En Rock 3 es redundante (M13 y su única task M13-t1 ya están ambas marcadas deferred), así que hoy no cambia ningún conteo; la regla existe para planes futuros donde solo se marque el milestone. | El schema de §2 solo define deferred en task, pero el fragmento ilustrativo de §2 y el seed marcan M13 (milestone) como deferred. Se fija la semántica antes del motor (Fase 2) porque afecta §4.6 (rollup) y §5.1 (denominador). | Bernardo | Locked |
| D-016 | 2026-08-13 | Se resuelve D-005: se mantienen ambos archivos (Opción B). sprint-plan.json en la raíz del repo es producción (copia del contenido de Rock 3, conforme al §10); data/rock3-seed.json se conserva como fixture de prueba para el motor de fechas. | D-005 quedó abierto en la sesión de scaffold; ahora que arranca la Fase 1 (carga + validación) había que fijar la ruta de producción antes de escribir el fetch. | Bernardo | Locked |
| D-015 | 2026-08-12 | v1 does not edit plan structure in-app. To change the plan (Rocks, milestones, tasks, durations, dependencies), regenerate `rock3-seed.json` and re-commit it. The dashboard reads structure, never writes it. | Keeps v1 simple and keeps a single source of truth for structure (the JSON). In-app structure editing is a large UI surface deferred out of v1. | Bernardo | Locked |
| D-014 | 2026-08-12 | Vista 4 (dashboard auto-reading task status from the Rock Projects) is deferred, not built in v1. Downstream, post-build: retire Asana, teach the Project Builder to emit the JSON, and make sprint planning always produce Rock-3-level detail. | The Project Builder confirmed Rock Projects store NO machine-readable per-task status in GitHub — only milestone-level prose and authoritative state in Asana. So the dashboard is the source of truth for task status in v1 (manual marking), which is universal and task-level. If Vista 4 is ever pursued, the clean bridge is Claude Code writing a status.json with the dashboard's task ids. | Bernardo | Replaced by D-100 |
| D-013 | 2026-08-12 | v1 includes a "This Week" view (three buckets per person: done this week / working on / not started, week boundary Friday-after-L10 → Thursday), a manual pin (drop-down; drag-and-drop is later polish), and a deliverable-link box per task. | The This Week view is the mid-week accountability screen and the backbone of the restructured L10. The pin lets a person override the projection (pull a future task forward / postpone one). The link box makes the dashboard the single home for every deliverable. Operations week (Fri→Thu) is deliberately distinct from CoachPulse's coaching week (Thu→Wed). | Bernardo | Locked |
| D-012 | 2026-08-12 | Every manual pin move is validated by the engine first: pulling a task forward is refused if any dependency (across people or across Rocks) is not done — the dashboard names the missing dependency and its owner; postponing a task shows the downstream cascade before confirming. | A task can look available but still depend on someone else's unfinished work. The drop-down lists only engine-validated available tasks; a blocked task never appears as pullable. Reuses the same dependency resolution the date engine already does. | Bernardo | Locked |
| D-011 | 2026-08-12 | Progress is a duration-weighted bar (weight = workDays only, not wait time). On-track is a burn-up: actual completed work vs. the plan's expected completed work for today, against a frozen baseline. The Rock is the primary unit for both metrics; the sprint-wide roll-up is a small secondary summary. | Weighting by task count would mislead (a 21-day task must count more than a 1-day one). Comparing against the frozen plan curve (not flat calendar time) avoids false "behind" alarms during waits like t-shirt shipping, because the planned curve is also flat there. | Bernardo | Locked |
| D-010 | 2026-08-12 | The task board displays tasks grouped by milestone with a checkbox each, in no imposed order. Marking is what people do; the dashboard stores and shows, it does not direct the work. Task status enum: open / in_progress / done. | The Rock Project guides the work; the dashboard records state. The engine's priority ordering is used only internally to project dates, and is never shown on screen. | Bernardo | Locked |
| D-009 | 2026-08-12 | Identity is a person-selector (no auth), reusing the testimonial-dashboard pattern plus three improvements: single People source of truth (a People tab read by frontend and Apps Script), server-generated stable row id, and write-then-verify. Structure lives in the seed JSON; state lives in a single append-only Events log in a Google Sheet, keyed by task id. | Multi-user marking (everyone marks their own tasks, Bernardo verifies mid-week and at L10) requires persistence and identity, ruling out a static HTML. The testimonial pattern is proven (GitHub Pages + Sheet + Apps Script). Events log holds status, deliverable link, and pin as actions. | Bernardo | Locked |
| D-008 | 2026-08-12 | Dates are computed outputs, never typed. The engine schedules from workDays + waitDays + dependencies + per-person capacity (one thing at a time; "Both" occupies both queues). When a person has several ready tasks, the engine auto-orders by priority (unblocks others > hard deadline > longest) for projection ONLY; executionOrder in the JSON is an optional override. Real completion timestamps always reproject over the guess. | This is the reverse-engineering-from-durations model. The invisible ordering never appears on screen; reality overrides both the pin and the priority guess. | Bernardo | Locked |
| D-007 | 2026-08-12 | Data model is Sprint → Rock → Project → Milestone → Task. Task is the atomic unit: owner (a person or "Both"), type (work / meeting / approval), workDays, waitDays (calendar wait during which the owner is free), dependsOn and crossDependsOn (may point to any task or milestone in the sprint). Fractional durations allowed. Deferred tasks excluded from projection and the progress denominator. | Captures parallelism, wait time, cross-Rock/cross-person dependencies, and meeting/approval task types — validated against the real Rock 3 (6 projects, M1–M23, 47 tasks, 40.75 work-days). | Bernardo | Locked |
| D-006 | 2026-08-12 | Project runs on the Chat + Code + GitHub pattern in ONE repo (`F4LA/OperationsDashboard`): code + `/docs` memory (spec, decision log, project brain) + `/data` seed, mirroring the testimonial-dashboard layout. A "build" Code session writes code; a dedicated "push" Code session writes the `/docs` memory. A dedicated Claude Project thinks/specs/decides/reviews and pulls `/docs` at chat start. | Chosen over a separate memory repo after seeing that the testimonial-dashboard keeps memory and code together in one repo and it works. Two Code sessions on the same repo don't collide because each edits different files and always pulls before pushing. | Bernardo | Locked |
| D-005 | 2026-08-12 | Seed data committed this session at `data/rock3-seed.json`, not at the repo root as `sprint-plan.json`. | Spec §10 states the production raw-URL path is `.../main/sprint-plan.json` at repo root. This session is scaffold-only (no fetch/engine logic yet), so the seed was placed where the setup instructions asked. **Revisit when §9 step 1's load/validate logic is built** — decide then whether to move/rename to match §10, or keep both a root `sprint-plan.json` (production) and `data/rock3-seed.json` (test fixture). | Bernardo (via setup instruction) | Superseded by D-016 |
| D-004 | 2026-08-12 | Stub modules scaffolded (empty, no logic): `dashboard/config.js`, `dashboard/validate.js`, `dashboard/engine.js`, `dashboard/events.js`, `dashboard/metrics.js`, `dashboard/board.js`, `dashboard/network.js`, `dashboard/thisweek.js`, plus root `app.js` and `styles.css`. | One stub per spec piece (§2–§6); `styles.css` added to match CoachPulse's file layout even though not explicitly requested. | Bernardo / Claude Code | Locked |
| D-003 | 2026-08-12 | Window-global prefix for every module is `OpsDash` (e.g. `OpsDashConfig`, `OpsDashEngine`, `OpsDashBoard`). | Mirrors CoachPulse's own `root.CoachPulse<Module>` attach pattern; kept short per that precedent. | Bernardo | Locked |
| D-002 | 2026-08-12 | Module stubs live in `/dashboard`, with `app.js` as the top-level bootstrap/orchestrator at repo root (not `/js`). | CoachPulse (the reference dashboard, inspected on disk) uses `/dashboard` + root `app.js`. Matching the real convention over the informal `/js` suggestion. | Bernardo | Locked |
| D-001 | 2026-08-12 | Repo created as `F4LA/OperationsDashboard`, public, PascalCase. | Matches the naming convention of the other Strong Standard dashboards and spec §10's stated repo name. | Bernardo | Locked |
