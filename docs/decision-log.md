# Decision Log — Operations Dashboard

Append-only. Every time the spec (`spec.md`) is ambiguous or silent and a call is made, it gets a new row here. Never edit or delete a past row — if a decision is reversed, add a new row that supersedes it and mark the old one's Status accordingly. New rows go at the TOP of the table, directly under the header. IDs are sequential.

| ID | Date | Decision | Context | Who | Status |
|---|---|---|---|---|---|
| D-074 | 2026-08-14 | Regla del smoke test: assertNoRow detecta EXISTENCIA de una fila con ese Task ID en la cola de Events, no NOVEDAD, así que todo caso de rechazo que la use debe pasarle un id único por corrida. Corolario: cuando el namespace obliga a un id fijo (T-NNNN son cuatro dígitos exactos; WEEK- es un lunes ISO), la unicidad por corrida es imposible por construcción y esos casos NO deben recibir opts.taskId — deben saltear la verificación en vez de "completarla". Queda documentado en el comentario de la función. | Un caso usaba el id real M2-t1 y falló contra eventos de fases anteriores que ya estaban en el Sheet: el rechazo había funcionado bien y el detector de fugas se disparó contra datos viejos. La auditoría encontró otras dos con la misma forma (T-0001, que es literalmente el primer id que asigna createTask, y WEEK-2026-08-17, que el happy path escribe de verdad); no rompían todavía solo porque no pasaban opts.taskId. Corregidas preventivamente a un centinela T-9999 y a WEEK-2026-09-07. | Bernardo | Locked |
| D-073 | 2026-08-14 | Aclaración a D-066(a): "un id nunca se reusa" aplica a los ids que llegaron a una fila persistida en Tasks. Un id quemado por una escritura fallida (el pin entró, la fila de Tasks no) SÍ se reasigna en el intento siguiente, y es inofensivo: el pin huérfano apunta a un id sin fila y nunca se une en el fold, y cuando ese id se reasigne, el createTask nuevo escribe su propio pin que pliega después por Timestamp y gana. | El mensaje de TASK_ROW_APPEND_FAILED dice "the id will be reassigned", que contradice la letra de D-066(a). El código es correcto; el texto del log era el que estaba mal redactado. | Bernardo | Locked |
| D-072 | 2026-08-14 | Ratificadas las cuatro decisiones que BUILD tomó por vacío del spec en la parte 1: (a) un confirmWeek sin Note se RECHAZA, nunca se lee como lista vacía — una semana sin compromisos manda "[]" explícito, porque un denominador vacío por accidente y uno vacío a propósito tienen que verse distinto (mismo espíritu que la prohibición de truncar en D-070); (b) BAD_VALUE_WEEK se agrega como código propio: un week presente pero no lunes reportaba MISSING_WEEK, mensaje activamente engañoso; (c) MAX_NOTE_LEN = 5000, aplicado SOLO a confirmWeek, que es donde D-070 lo pide; (d) TASK_ROW_APPEND_FAILED como código del caso en que el pin entra y la fila de Tasks no. QUEDA ABIERTO, deliberadamente: el Note de discard y cancel sigue sin tope, contra una celda de Sheets que corta a 50.000 caracteres — un pegado enorme daría una excepción cruda en vez de un rechazo nombrado. Se resuelve en la parte 2, donde el frontend va a limitar el input de todas formas. | Backend de la Fase 8 (commit 5262844). Las cuatro se revisaron contra el código real y la regresión, no contra el reporte. Procedimiento manual (C) ejecutado el 2026-08-14: dos createTask en paralelo devolvieron T-0005 y T-0006, filas 6 y 7, ambas con su pin y verified:true. Los timestamps quedaron separados por ~3 segundos y la segunda petición lanzada respondió primero — el LockService serializó en vez de dejar que las dos leyeran el mismo máximo de la columna y asignaran el mismo id. | Bernardo | Locked |
| D-071 | 2026-08-14 | Reparto de código de la Fase 8. (a) dashboard/thisweek.js SE EXTIENDE, no se reescribe: opsWeek recibe un offset opcional (-1 / 0 / +1 → semana que cierra / actual / que abre) manteniendo válida su firma actual; buckets sobrevive como la matemática de medición del §12 aunque su render de tres columnas se retire; cascadeOf queda intacta y pasa a alimentar también la previsualización de cancelación (D-068). (b) availableToPull CAMBIA DE CONTRATO por el §11.5: debe devolver también las tareas bloqueadas, marcadas y nombrando su bloqueador y el dueño del bloqueador. Esto SUPERSEDE a D-063(e), que decía exactamente lo contrario. Las aserciones de tests/thisweek.test.js que verifican que una tarea bloqueada NO aparece deben reescribirse para verificar que aparece marcada — es un cambio de expectativa por decisión de spec, no un test aflojado para que pase. (c) Se crea dashboard/todos.js para el markup de la vista §11 y la matemática del §12; las funciones renderTw* de board.js que el §11 deja obsoletas se retiran. Motivo: board.js ya pesa 56 KB y el patrón de la casa (D-053) separa matemática de markup, no obliga a que todo el markup viva en un solo archivo. (d) La burn-up NO se toca salvo por lo que exige D-068(e). | Pregunta abierta del handoff de Fase 8. Se trazó el código real antes de decidir: las cuatro funciones puras de thisweek.js envejecieron bien y solo una cambia de contrato, así que reescribir habría tirado 67 tests para reconstruir lo mismo. Bernardo preguntó además si la burn-up se podía borrar por no usarla; se resolvió que no se toca — el pie numérico obligatorio de D-053 ya le da el veredicto que quiere ("cuánto atrasados") y la curva agrega CUÁNDO empezó el desvío a costo cero por estar ya construida. Se revisa tras un sprint de uso real. | Bernardo | Locked |
| D-070 | 2026-08-14 | Congelamiento del denominador del §12 con un evento nuevo confirmWeek y CERO maquinaria de fold nueva. Forma: Task ID = WEEK-<lunes ISO> (no vacío, estable, foldeable), Action = confirmWeek, Value = el lunes ISO validado con el mismo validador que ya usa pin, Note = el array JSON de los task ids congelados. Como el fold de D-009 es por (Task ID, Action) tomando el último por Timestamp, re-confirmar una semana REEMPLAZA el denominador sin escribir una línea de fold nueva, y todas las confirmaciones previas quedan en el log. El servidor impone un tope de largo sobre el Note y RECHAZA ruidosamente si se pasa: nunca truncar. | Se descartó la alternativa de reconstruir el denominador reproduciendo el log al instante de la confirmación, y el motivo es la razón por la que esta fila existe: una tarea de Rock que el motor propuso y que nadie tocó NO TIENE NINGÚN EVENTO. El log no la contiene, así que reproducir daría un denominador que ignora justo las tareas que salieron bien. Se descartó también una pestaña Weeks propia: agrega una tabla y un segundo mecanismo de escritura para algo que el log append-only ya resuelve. El tope con rechazo ruidoso es deliberado — un denominador truncado a la mitad no se ve roto, se ve como que la gente rindió el doble. | Bernardo | Locked |
| D-069 | 2026-08-14 | Reversión de las acciones destructivas: discard/undiscard y cancel/uncancel se implementan como PARES, espejando exactamente el par pin/unpin que ya existe — el miembro negativo pliega sobre el mismo slot del fold que el positivo, con Value vacío y Note opcional. No se agrega maquinaria nueva de fold. | Sin par de reversión, un clic errado durante el L10 (pantalla compartida, alguien marcando por otros, ritmo de reunión) sería permanente y solo corregible editando el Sheet a mano. El precedente de pin/unpin ya resolvió esta forma y repetirla cuesta casi nada, porque el validador del miembro negativo es el mismo. | Bernardo | Locked |
| D-068 | 2026-08-14 | Se puede CANCELAR una tarea de Rock desde la app. SUPERSEDE la regla del §11.4 que decía "Rock task: move to next week only. No discard." La cancelación es una acción DISTINTA del discard de una ad-hoc y lleva nombre distinto (cancel, no discard) porque es un acto distinto: tirar una ad-hoc no afecta a nadie, cancelar una de Rock cambia el plan del sprint y puede destrabar trabajo de otra persona. Reglas: (a) motivo obligatorio en el Note, igual que discard; (b) antes de confirmar se muestra la cascada de tareas afectadas calculada con cascadeOf (la misma de postergar, D-063d) — informativa, advierte y no bloquea; si no arrastra a nadie se cancela y listo, que es el caso que motivó la decisión; (c) una tarea cancelada sale del schedule en liveMode por el MISMO camino de código que una tarea deferred del plan (§4.6, D-017), así que sus dependientes quedan con la dependencia satisfecha. Es la primera modificación a engine.js desde la Fase 2 y por eso se especifica como reuso de un camino existente, no como concepto nuevo; (d) los work-days cancelados salen del denominador de la barra de progreso (§5.1) y se muestran al lado como total cancelado: no se esconden y no se dejan adentro, porque dejarlos adentro haría que el Rock nunca llegue a 100% y el semáforo quede rojo para siempre por trabajo que se decidió no hacer — el hábito que D-058 declaró el peor posible para un tablero nuevo; (e) la curva PLANIFICADA de la burn-up NO se recalcula al cancelar, sigue congelada per D-053, y el pie numérico nombra los días cancelados aparte. La barra de progreso contesta "cuánto del trabajo que queda está hecho", la burn-up contesta "cómo vamos contra lo que prometimos", y reescribir la promesa a mitad de sprint deja al gráfico incapaz de mostrar que se prometió de más; (f) NO se construye cancelación masiva de un milestone o un proyecto entero: si muere una línea completa, eso ya no es "una tarea dejó de tener sentido" sino que el Rock cambió, y eso se arregla regenerando sprint-plan.json, que es donde vive el plan. Un botón de cancelar-todo-esto convertiría al dashboard en editor del plan, que es justo lo que el §0 dice que no hace. Se revisa si pasa dos veces en un sprint real. | Bernardo lo planteó al revisar el §11.4: en la práctica una tarea de Rock puede dejar de tener sentido a mitad de sprint, y si el dashboard no deja cancelarla hay que arrastrarla de semana en semana hasta que termine el sprint — basura visible cada viernes en la única pantalla que se quiere que el equipo mire. Se evaluaron tres alcances: (A) todo en la Fase 8; (B) solo el evento y la desaparición de la vista, dejando progreso/burn-up para después; (C) no construirlo. Se eligió A. B se descartó porque dejaría el semáforo contando trabajo que ya se decidió no hacer, y un semáforo que miente es peor que un botón que falta. | Bernardo | Locked |
| D-067 | 2026-08-14 | Evento discard para tareas ad-hoc: se agrega a las acciones aceptadas por el backend, con Value vacío y Note OBLIGATORIA (el motivo), rechazada con código propio si viene vacía. El servidor rechaza además un discard cuyo Task ID no matchee ^T-\d{4}$: el §11.4 prohíbe descartar una tarea de Rock, y si esa regla vive solo en el frontend se saltea con una llamada directa al Web App, que no tiene restricción de origen. Es una excepción ESTRECHA a D-040 ("el Task ID no se valida contra el plan"): no se valida contra el plan, se valida contra el namespace, que el servidor sí conoce sin tener el JSON. Segunda mitad: discarded NO es un valor de setStatus. El §1 lo lista dentro del set de status de una ad-hoc, pero el §3 fija setStatus ∈ {open, in_progress, done} y el backend lo implementa así; se resuelve que discarded es un estado DERIVADO de la presencia de un evento discard en el fold. Una sola forma de descartar, no dos. Lo mismo vale para cancelled respecto de D-068. | Pregunta abierta del handoff de Fase 8. La regla del namespace apareció al notar que el servidor puede enforcear la asimetría del §11.4 sin conocer el plan, que era justamente la razón por la que D-040 había renunciado a validar el Task ID. | Bernardo | Locked |
| D-066 | 2026-08-14 | Creación de tareas ad-hoc: acción RPC nueva createTask, hermana de appendEvent y no un quinto evento — escribe en otra pestaña con otro esquema. Garantías del servidor: (a) id asignado por el servidor en el namespace T-NNNN, leyendo la última fila de Tasks e incrementando, bajo el mismo LockService que ya serializa las escrituras, así que dos personas creando a la vez en el L10 no colisionan; un id nunca se reusa, ni siquiera tras un discard; (b) week es OBLIGATORIO y el servidor escribe la fila de Tasks Y appendea el evento pin bajo el mismo lock, con lo cual "toda tarea nace con una semana" (§11.6) pasa a ser garantía del servidor y no convención del frontend; (c) validaciones: desc no vacío, owner exactamente uno y activo en People (reusando checkActor_ aplicado al owner además del actor) con rechazo NOMBRADO de "Both" por código propio, workDays numérico > 0 con fracciones, deadline ISO opcional, sourceIssueId opcional; (d) header guard posicional sobre la pestaña Tasks, mismo patrón de D-033; (e) write-then-verify releyendo las últimas filas de Tasks, igual que Events. sourceIssueId se acepta y se guarda desde ya aunque el §13 no exista: cuesta nada hoy y evita migrar la pestaña Tasks cuando llegue la Fase 9. | Pregunta abierta del handoff de Fase 8: el backend solo sabía appendear al Events log. El punto (b) se decidió sobre un riesgo concreto: con createTask y pin como dos llamadas separadas, un fallo de red entre ambas deja una tarea que existe y no está en ninguna semana — invisible en todas las vistas y sin forma de recuperarla, porque el §11.6 decidió a propósito que no hay backlog donde buscarla. No rompe la regla del §3 de que el Events log no se extiende estructuralmente: el pin es el pin de siempre. | Bernardo | Locked |
| D-065 | 2026-08-14 | Verificación de estado: cuando exista un hash de commit, verificar el contenido pidiendo el archivo POR HASH (raw.githubusercontent.com/F4LA/OperationsDashboard/<hash>/<ruta>) y no por la rama (/main/<ruta>). Un fetch de la rama devuelve caché vieja incluso con parámetro de query anti-caché; una URL por hash es única y nunca estuvo cacheada, así que refleja la verdad del commit. Solo si el hash confirma el contenido y aun así la rama difiere, se puede concluir propagación pendiente — nunca push fallido. | Pasó dos veces el mismo día. Primero con index.html: se leyó la rama, se concluyó que la Fase 5 había salido sin subir el parámetro de versión, y se hizo un commit de arreglo (471e8db) para un bug que ya estaba cerrado desde 06b568d — sin daño, pero trabajo inútil y una afirmación falsa registrada. Después con spec.md: la sesión PUSH reportó el commit 677d016 aplicado, el chat leyó la rama, vio v1.1 y 38515 bytes, y planteó que el push no había llegado; la lectura por hash devolvió 45818 bytes y v2.0, confirmando que el push estaba perfecto y la rama servía caché vieja. En ambos casos la sesión de Code tenía razón y la lectura del chat estaba equivocada por el mismo motivo. Complementa D-056: verificar contra el archivo, sí, pero contra el archivo del commit correcto. | Bernardo | Locked |
| D-064 | 2026-08-14 | Alcance preciso del pin, fijado al construir la Fase 7 y verificado en producción. (a) El pin gobierna EXCLUSIVAMENTE el bucket "Not started": no puede sacar una tarea de "Done this week" ni de "Working on", porque esos dos los gobierna el status real y un pin no puede des-terminar una tarea ni ocultarla mientras está en curso. El §6.3 solo habla de traer una tarea futura o posponer una, o sea el caso open/no-empezada. (b) Se confirma en uso real la consecuencia de D-063(c): posponer mueve la tarea fuera de la lista de esta semana pero NO cambia su fecha proyectada, así que la misma tarea puede seguir mostrándose con fecha de esta semana en el Sprint Board. Es el comportamiento decidido, no un bug; se revisa si molesta tras un sprint de uso real. (c) Una tarea owner:"Both" se muestra en las dos columnas per D-063(b), y postergarla desde cualquiera de las dos la posterga para ambos: es una tarea mostrada dos veces, no dos tareas. | Al implementar D-063(c) apareció que "el pin es un override de pertenencia al bucket" no decía SOBRE CUÁL bucket. Se resolvió en el momento y quedó verificado en el navegador antes de comitear; se registra ahora para que no se re-decida. Durante esa misma prueba salió un bug real que los tests no agarraron: la firma del repintado comparaba solo qué tareas había en cada bucket, así que pinear una que ya estaba ahí no repintaba su marcador ni su botón Release. Arreglado plegando el estado de pin de cada tarea en la firma. Precedente: probar en el navegador agarra lo que la suite no. | Bernardo | Locked |
| D-063 | 2026-08-14 | Contrato de la vista This Week (§6.3), fijado antes de construir. (a) BUCKETS: "Done this week" = tareas con status done y statusChangedAt truncado (D-028) dentro de la ventana; "Working on" = status in_progress, sin filtro de ventana; "Not started" = status open cuya proyección viva (§4.7) se solapa con la ventana, es decir plannedStart <= windowEnd Y plannedFinish >= windowStart. (b) Una tarea owner:"Both" aparece en la columna de CADA persona de owners[], no en una columna "Both" propia — la vista es de compromiso personal, y un compromiso conjunto lo es de los dos. (c) EL PIN NO MUEVE FECHAS: verificado que la firma del motor es liveMode(plan, currentState, todayISO) y no recibe pins, así que en v1 el pin es exclusivamente un override de PERTENENCIA AL BUCKET — trae una tarea futura a la lista de esta semana o la saca, sin tocar la proyección ni el chip on-track ni la burn-up. "Postergar" = pin al lunes de la semana siguiente; "traer" = pin al lunes de esta semana; "soltar" = unpin y vuelve a la proyección automática. (d) La cascada downstream que el §6.3 exige mostrar antes de postergar se calcula con el fan-out de dependencias de validate.js (D-024), se presenta como lista de tareas afectadas con su dueño, y es informativa: advierte, no bloquea. (e) El dropdown de "+ add to this week" lista solo tareas open cuyas dependsOn y crossDependsOn estén TODAS en done según currentState; una tarea bloqueada no aparece. | El §6.3 describe la vista en prosa pero deja sin definir el criterio exacto de cada bucket, el tratamiento de "Both", y sobre todo si el pin es un override de presentación o una entrada del motor. Lo último importa: si el pin moviera fechas, el motor dejaría de ser la única fuente de la proyección y el chip on-track podría falsearse a mano, que es justo lo que D-023 decidió no permitir. Se elige el override de presentación: la persona declara su compromiso, el motor sigue diciendo la verdad. Revisable si un sprint real pide que el pin reproyecte. | Bernardo | Locked |
| D-062 | 2026-08-14 | Navegación entre vistas: Opción B — control segmentado ("Sprint board" / "This week") en la topbar, a la izquierda del selector de persona. Elegido viendo tres mockups con datos reales de Rock 3 (mismo patrón de D-048 y D-051). La vista activa se persiste en localStorage con la clave `opsdash.view` (misma convención que `opsdash.actor`, §3), default "board" si no hay valor o el valor no es válido. Consecuencia aceptada a propósito: cuando entre la Fase 6 el control queda con tres segmentos y probablemente haya que migrar a tabs — se acepta ahora y se revisa entonces, en vez de construir tabs para una tercera vista que todavía no existe. | Quedó sin resolver al planear la Fase 6 y aterrizó en la Fase 7 porque This Week es la primera segunda pantalla real. Bernardo no tenía preferencia en abstracto; eligió sobre mockups. B gana por no consumir altura vertical permanente en una pantalla que ya carga topbar, summary bar y burn-up antes del primer Rock. | Bernardo | Locked |
| D-061 | 2026-08-14 | Semana de operaciones y clave canónica de semana, escritas por primera vez. La ventana se calcula desde `opsWeekStartDay` de la Settings (valor real verificado en el Sheet: la cadena "Friday", nombre de día en inglés): windowStart = el opsWeekStartDay más reciente en o antes de hoy; windowEnd = windowStart + 6 días CALENDARIO (jueves). La CLAVE de la semana — el único valor que viaja en un evento pin — es el lunes ISO contenido en esa ventana (con inicio viernes, windowStart + 3). Fundamento de por qué el lunes es una clave válida aunque no sea el inicio de la semana: cualquier ventana de 7 días contiene exactamente un lunes, así que la clave es única y sigue siéndolo si algún día cambia opsWeekStartDay. Parseo: nombre de día en inglés (case-insensitive) → índice; si el valor falta o no parsea, fallback a Friday, documentado en el call site y nunca silencioso (mismo patrón que el fallback de band en app.js línea 130). | El §3 del spec YA fija que el Value de un pin es el lunes ISO de la semana, y backend/Code.gs (líneas 329-345) solo implementa eso, rechazando con BAD_VALUE_PIN cualquier otro día. Pero el §6.3 corre la semana de viernes a jueves y en ningún lado estaba escrito el mapeo entre una y otra, ni cómo se computan los límites desde la Settings. Sin esta fila, el primer pin del frontend habría sido rechazado por el servidor o —peor— habría mandado un lunes de la semana equivocada. Además queda registrado que opsWeekStartDay se leía en app.js y se descartaba: hasta hoy ningún código lo consumía. | Bernardo | Locked |
| D-060 | 2026-08-14 | Las dependencias no cruzan Rocks: se mezclan dentro del mismo Rock, no entre Rocks (regla vigente, revisable). `crossDependsOn` sigue soportado por el validador y el motor, pero queda sin ejercitar. Consecuencia para la Fase 6: los "conectores cross-Rock" que pide el §6 no van a tener nada que dibujar, así que ese requisito queda en suspenso, no cancelado. Lo que SÍ cruza Rocks es la capacidad de las personas: verificado contra engine.js (líneas 338-339) que availableFrom es un único mapa por persona para todo el sprint, no uno por Rock, así que una persona con tareas en dos Rocks se agenda de a una a la vez sin cambios en el motor. | Al planear la Fase 6 se asumió que con 4 Rocks habría dependencias cruzadas. Bernardo confirmó que no: el entrelazado ocurre dentro de cada Rock. Registrarlo evita construir un conector que nunca se dibuja y deja explícito el único acoplamiento real entre Rocks, que es de recurso y ya funciona. | Bernardo | Locked |
| D-059 | 2026-08-14 | Se invierte el orden del §9: la Fase 7 ("This Week" + pin manual) se construye ANTES que la Fase 6 (Network/Timeline). Razón: los dos rituales reales de la empresa — el L10 del viernes y el chequeo de Bernardo del miércoles — apuntan a This Week, que el propio §6.3 llama el backbone del L10 reestructurado; ningún ritual pide un Gantt. Además la Fase 6 queda con una PRECONDICIÓN explícita: hoy el dashboard tiene cargado 1 Rock de los 4 que corre el sprint, y una timeline dibujada sobre un cuarto del sprint no muestra lo único que la justifica (cuál Rock arrastra a cuál). Orden resultante: Fase 7 → carga de los 4 Rocks → Fase 6. La Fase 6 no se cancela, se difiere hasta tener con qué. | Bernardo frenó a preguntar si la timeline realmente sumaba. En el análisis, el chat afirmó erróneamente que el sprint tenía un solo Rock — leyó el sprint-plan.json (que contiene uno) y concluyó sobre la realidad de la empresa (que corre cuatro). Bernardo corrigió. La corrección devuelve valor a la Fase 6 pero no cambia el orden, porque con 4 Rocks el chequeo del miércoles se vuelve más necesario, no menos. Precedente: trazar un archivo no autoriza a afirmar sobre el mundo. | Bernardo | Locked |
| D-058 | 2026-08-14 | Arranque del tracking en S3-2026: se elige Opción A — backfill. El equipo carga en el board las tareas ya completadas desde el 2026-07-27 en vez de esperar a S4 con el semáforo en rojo permanente. Consecuencia aceptada a propósito: el Timestamp es server-side (D-041), así que TODO lo retroactivo queda fechado 2026-08-14 y la curva "actual" de la burn-up queda plana desde el inicio del sprint y da un salto vertical ese día. No es un defecto de §5.2 — el dashboard efectivamente no sabía nada antes de esa fecha. Se explica en voz alta en el L10 ("la curva salta hoy porque hoy cargamos el historial") en vez de disimularlo. Cada persona marca SUS propias tareas: el Events log guarda el actor, y un backfill hecho por una sola persona falsearía la autoría. | El board nació en la mitad de un sprint que venía corriendo en Asana desde el 27 de julio, así que al abrirse mostraba 0% y "Projected finish 2026-10-05" — exactamente 15 días laborales después del 2026-09-14 del plan, que son los 15 días laborales transcurridos del sprint con cero marcas. El motor decía la verdad sobre lo que sabía; lo que faltaba era el historial. Un rojo sin explicar enseña al equipo a ignorar el semáforo, que es el peor hábito posible para un tablero nuevo. | Bernardo | Locked |
| D-057 | 2026-08-14 | Regla de la casa: todo commit que toque un archivo .js o styles.css sube el parámetro de versión de TODOS los assets en index.html (?v=YYYYMMDD<letra>). Se incluye el <link> de styles.css, que hasta la Fase 5 no llevaba parámetro y ahora sí. | Descubierto al cerrar Fase 5: el commit 45f8afd agregó el div del panel de burn-up a index.html sin subir el ?v=, así que cualquier navegador con caché de Fase 4 hubiera recibido el HTML nuevo con board.js/metrics.js viejos — panel vacío y cero repintado. La sesión BUILD extendió la regla al CSS por su cuenta y con razón: .burnup-row{height:34px} vive en el CSS nuevo, así que un styles.css cacheado colapsa ambas gráficas a altura cero, mismo síntoma por otra puerta. | Bernardo / Claude Code | Locked |
| D-056 | 2026-08-14 | CORRIGE EL REGISTRO DE D-052: esa fila afirma que BUILD ajustó el contraste del flag de overshoot a ≥4.5:1 AA, pero ese cambio de color nunca llegó a commitearse — de aquel pase solo quedó el bump de tamaño del flag de 18px a 24px. El flag siguió en 4.41:1 hasta hoy. Corregido ahora en el commit 06b568d: --red pasa de #C13B3B a #B83434, que da 4.87:1 sobre --red-bg y 5.87:1 sobre blanco. Verificados los nueve consumidores de --red: tres pasan de 4.41 a 4.87, ninguno baja, y toast-error (blanco SOBRE --red) también mejora porque oscurecer el fondo sube el contraste contra texto blanco. D-052 queda Locked pero parcialmente inexacta; esta fila es el registro correcto. | La sesión BUILD lo detectó revisando el historial de styles.css al arrancar Fase 5, y el chat de diseño lo confirmó calculando el ratio real. Una fila del log que da por hecho algo que no se commiteó es peor que no tenerla: hace confiar en una verificación que nunca ocurrió. Vale como precedente — un cierre de fase se verifica contra el archivo, no contra el reporte. | Bernardo / Claude Code | Locked |
| D-055 | 2026-08-14 | Fase 5 (Métricas, §5) CERRADA y probada en el sitio desplegado (f4la.github.io), estándar D-049. Commits 45f8afd (las tres partes) + 06b568d (arreglos de cierre). Entregado: (A) OpsDashMetrics.burnupSeries — un punto por día calendario, planned desde el plan congelado, actual desde statusChangedAt truncado y null después de hoy, per D-053; (B) gráfica SVG inline en dos scopes (sprint-wide bajo la summary bar, por Rock dentro del header) con línea punteada/sólida, marca de hoy, pie numérico y role="img"; (C) diffAndRepaint por firma de tarea/milestone/Rock, per D-054. Regresión 181/181 (157 previos + 24 de tests/burnup.test.js). engine.js, validate.js y events.js sin tocar. Dos mejoras que BUILD aportó sobre la especificación y quedan adoptadas: el axisEnd se calcula POR SCOPE (el desborde de un Rock no estira el eje de los demás) y la protección de filas se dispara solo con input enfocado o con texto tipeado (la primera versión hubiera protegido todas las filas siempre, porque toda tarea sin deliverable muestra un input vacío). | Evidencia del E2E en producción: con cinco tareas marcadas, el milestone M2 pasó de 2026-09-21 a 2026-09-18, M3 de 2026-09-25 a 2026-09-22, y la tarea "Lock the refund rate target %" (owner Both, nunca tocada, sigue en Open) se movió sola de 2026-09-21 a 2026-09-18 — la reproyección cross-dependencia visible en pantalla, que es exactamente el defecto que D-054 arregla. Rastro de prueba borrado a mano del tab Events. | Bernardo | Locked |
| D-054 | 2026-08-13 | Repintado tras marcar: se corrige un defecto encontrado leyendo el código en el chat de diseño de Fase 5 — recompute() sí recalcula liveMode en cada marca (§4.7), pero solo se repintaban la fila marcada, el header de su Rock y la summary bar, dejando toda fila de tarea y header de milestone downstream con su plannedFinish viejo hasta un Refresh. Solución elegida: diff dirigido (snapshot del liveResult previo, comparar firmas por tarea/milestone/Rock, patchear solo lo cambiado) en vez de un render() completo, porque el re-render remonta el <select> que la persona acaba de usar y le quita el foco. Guarda explícita: no patchear una fila con input de deliverable abierto o con foco. | La reproyección viva es la razón de ser del dashboard frente a Asana; recalcularla sin mostrarla la vuelve invisible. No estaba registrado como bug en ningún lado — apareció al trazar el código de Fase 4 contra §4.7 antes de escribir el prompt de Fase 5. | Bernardo | Locked |
| D-053 | 2026-08-13 | Burn-up (§5.2/§5.3) definida antes de construir: gráfica SVG inline chica, sin librería ni canvas, sin hover ni tooltips (board interno de 5 personas), con pie numérico obligatorio en texto plano ("Actual X of Y planned work-days — Z behind") que además cubre por sí solo la lectura del indicador. Matemática en metrics.js como función pura burnupSeries(frozenTasks, currentState, taskIds, axisStart, axisEnd, today) → un punto por día CALENDARIO; planned(D) desde el plan congelado (nunca recalcular planMode); actual(D) desde statusChangedAt truncado (D-028) y null para toda fecha > today (no se dibuja futuro); total = Σ workDays no-deferred del scope = 40.75 para el sprint actual. Dibujo en board.js (markup es su dominio, matemática es de metrics.js). Eje X de sprint.start a max(sprint.end, plannedFinish congelado más tardío) = 2026-09-14 por D-023: cortar en sprint.end dejaría la curva planificada sin llegar al techo y se leería como un bug. Se renderiza en DOS scopes, ambos siempre visibles: sprint-wide bajo la summary bar y por Rock bajo la fila de métricas del header. | El chip on-track ya resuelve el veredicto de §5.2 con los dos escalares en "hoy"; §5.3 lista la gráfica como opcional, así que el tamaño del esfuerzo era una decisión abierta. Se elige el punto medio: la curva agrega CUÁNDO empezó la desviación, que los números solos no dan, sin el costo de un chart interactivo. Duplicar la gráfica en Rock y sprint es redundante hoy (Rock 3 es el único Rock del sprint) y se acepta a propósito: el componente se escribe una vez y en un sprint de 2-3 Rocks es lo que permite ver cuál arrastra. | Bernardo | Locked |
| D-052 | 2026-08-13 | Rediseño visual del Sprint Board (D-051) CERRADO. BUILD ajustó contraste del flag de overshoot a ≥4.5:1 AA y agregó separación visual entre Project/Milestone (margin+padding en .project-divider/.milestone-group, además de la corrección de aire pedida tras revisión visual de Bernardo). Confirmado E2E en navegador real (f4la.github.io): dropdown de status escribe y verifica contra el Sheet real (M1-t1, in_progress→open, actor Bernardo, filas de prueba borradas a mano). Regresión 157/157 intacta. | Segunda vuelta de feedback visual de Bernardo tras ver la primera versión del rail — pidió más separación entre secciones; BUILD lo resolvió y lo confirmó contra el plan real antes de este cierre, mismo estándar que D-049 exigió para Fase 4. | Bernardo | Locked |
| D-051 | 2026-08-13 | Rediseño visual del Sprint Board decidido: Option B (rail izquierdo coloreado por nivel — navy 6px project, steel 6px milestone, border 6px task — con indentación creciente 16/26/44px) para la jerarquía Rock→Project→Milestone→Task. El control de status pasa de ciclo de 3 clics a <select> dropdown, mismo write path (postEvent("setStatus",...) + verify) intacto. La alerta de overshoot (D-023) se resuelve con tooltip custom en hover/focus (reemplaza el title nativo del navegador, poco visible) MÁS una leyenda estática siempre visible en la summary bar ("⚠ = projected past sprint end"), para que no dependa de encontrar el hover. Cero cambios de lógica — solo board.js + styles.css. BUILD corre web-design-guidelines contra ambos archivos antes de comitear. | Bernardo vio 3 mockups reales (A/B/C) generados con datos reales de Rock 3 en el chat de diseño y eligió B por decidir mejor viendo que en abstracto — mismo patrón de decisión que D-048. | Bernardo | Locked |
| D-050 | 2026-08-13 | Pase de rediseño visual del Sprint Board ANTES de Fase 5: se abre un chat de diseño dedicado porque la UI de Fase 4, aunque funcional, quedó plana (proyectos apenas distinguibles del texto de milestone, filas apeñuscadas, alerta ⚠ de overshoot sin explicación visible). Se rediseña ahora, no después, porque Fases 5/6/7 reúsan los mismos patrones visuales y heredarían el problema. Decisión ya tomada dentro del pase: el control de status pasa de ciclo de 3 clics (D-nada, era comportamiento aprobado en el reporte de Fase 4) a un dropdown de selección directa de estado. BUILD aplicará el skill web-design-guidelines y revisará strong-standard-web-audit al implementar. | Feedback de Bernardo al ver el board en vivo tras el E2E. El ciclo de 3 clics para volver a open era poco intuitivo; la jerarquía visual no comunicaba estructura. | Bernardo | Locked |
| D-049 | 2026-08-13 | Fase 4 (Sprint Board, Views 1+3) probada end-to-end en el navegador real desplegado (f4la.github.io/OperationsDashboard), no solo con mock: seleccionar actor, ciclar status de una tarea real (M1-t1) open→in_progress→done→open, y confirmar que verifyEvent se autoconfirma sin errores de consola — la mitad del write-then-verify que no se pudo probar desde Node por la restricción de referrer (D-043). Las filas de prueba (SMOKE-F4-t1 + las 3 de M1-t1) se borraron a mano del Sheet. Fase 4 QUEDA CERRADA Y PROBADA. | Cierre del E2E que quedó pendiente al final del reporte de BUILD (commit 1acc181). El POST real ya estaba probado; faltaba la autoconfirmación en su destino real. | Bernardo | Locked |
| D-048 | 2026-08-13 | Fase 4 — patrón visual: CoachPulse es la referencia de estética (tipografía, color, espaciado); la sesión BUILD lo inspecciona en disco antes de escribir styles.css, no lo copia literal. UI en inglés per §10. | Pregunta abierta #4 de Fase 4. | Bernardo / Claude Code | Locked |
| D-047 | 2026-08-13 | postEvent/verifyEvent (write path real del §3 traducido a cliente) viven en dashboard/events.js, no en un módulo nuevo — mismo dominio que el fold ya asignado ahí por D-036. Verify reintenta con backoff 0/500/1000/2000ms sobre las últimas 40 filas de Events antes de reportar VERIFY_TIMEOUT. | Pregunta abierta #2 de Fase 4; el spec §3 describe la mecánica pero no dónde vive el código cliente. | Bernardo / Claude Code | Locked |
| D-046 | 2026-08-13 | El frontend emite únicamente la forma shortcut del payload ({action, sprintId, taskId, value, actor, note}), nunca el envelope appendEvent. El backend sigue aceptando ambas (D-039) pero el board fija una sola forma como su uso canónico. | D-039 dejaba abiertas dos formas equivalentes; Fase 4 necesita fijar cuál emite el cliente real. | Bernardo / Claude Code | Locked |
| D-045 | 2026-08-13 | Confirmado: es seguro commitear en dashboard/config.js tanto la API key (restringida por referrer, D-035) como la Web App URL sin restricción de origen — una URL de cliente-navegador nunca es secreta (visible en devtools) con o sin commit, así que commitearla no agrega exposición real sobre la ya aceptada en §3. | Pregunta abierta #1 de Fase 4, reconfirmando la nota de seguridad del §3 antes de escribir el archivo. | Bernardo / Claude Code | Locked |
| D-044 | 2026-08-13 | Falso positivo corregido en el harness de D-037: la verificación "nada se escribió" devolvía PASS cuando la LECTURA fallaba (se ponía verde por quedarse ciega). Ahora una lectura fallida es inconclusive → se reporta como fallo, nunca como pass. Se agregó además un preflight que confirma lectura ANTES de escribir, para cortar en segundos ante un problema de credenciales en vez de escribir filas no verificables. | Descubierto al correr el smoke test real por primera vez. Un test que aprueba cuando no puede ver es peor que uno que falla; la regla vale para todo harness futuro del proyecto. | Bernardo / Claude Code | Locked |
| D-043 | 2026-08-13 | Tensión de diseño registrada: la restricción por referrer de la API key (§3) la vuelve inusable desde cualquier cliente que no sea navegador, y el harness de D-037 (Node) necesita leer con esa key. Resolución: el test manda el origen de GitHub Pages como header Referer; la key sigue restringida. No evade la restricción (un Referer es client-supplied en cualquier plataforma; la restricción nunca fue una barrera de auth, solo evita que una key copiada sirva en otra página). Es el primer choque del spec consigo mismo entre §3-lectura y §3-restricción. | Apareció al correr el smoke test: la key bien configurada para producción rechazaba al test con API_KEY_HTTP_REFERRER_BLOCKED. | Bernardo / Claude Code | Locked |
| D-042 | 2026-08-13 | setDeliverable acepta solo URLs http/https, tope 2000 chars; un javascript: u otros esquemas se rechazan con BAD_VALUE_URL. El valor se renderiza como link clickeable en el board (§6), así que un esquema peligroso ahí sería XSS almacenado. | §3 pide "URL bien formada" sin fijar esquemas permitidos; la decisión cierra el hueco de seguridad antes de que el board consuma el valor en Fase 4/5. | Bernardo / Claude Code | Locked |
| D-041 | 2026-08-13 | Robustez del Timestamp server-side: se escribe el ISO, se fuerza la celda de la columna Timestamp a formato texto (@) y se reescribe, porque Sheets auto-convierte un ISO a valor-fecha y la API lo devolvería como string con formato de locale, rompiendo el fold en silencio. El Timestamp lleva offset real (ej. -05:00), no hora local etiquetada como Z; por eso events.js ordena numéricamente (por instante), no por string —  un cambio de DST dentro del sprint movería el offset. | §3 pide timestamp server-side en timezone del Sheet pero no anticipa la auto-conversión de Sheets ni el efecto de DST sobre el ordenamiento del fold. | Bernardo / Claude Code | Locked |
| D-040 | 2026-08-13 | Columna Active en People: un Actor marcado inactivo se rechaza con ACTOR_INACTIVE (distinto de ACTOR_UNKNOWN); pero una celda Active vacía se trata como activo, para que un People a medio llenar no deje a todo el equipo afuera. El Task ID NO se valida contra el plan (§3 solo pide rechazar vacío, y el Apps Script no tiene el JSON). Todas las respuestas son HTTP 200 incluso los errores (ContentService no puede setear status); el frontend debe ramificar por el campo `ok`, nunca por el status HTTP. | §3 nombra la columna Active pero no fija su semántica; sin esto la columna no serviría. El resto son contratos server↔frontend que el §3 no explicita y la Fase 4 necesita conocer. | Bernardo / Claude Code | Locked |
| D-039 | 2026-08-13 | Forma del payload del write path (CONTRATO con el frontend de la Fase 4): el doPost acepta DOS formas equivalentes — el atajo `{action:"setStatus"|"setDeliverable"|"pin"|"unpin", ...}` y el envelope `{action:"appendEvent", eventAction:"setStatus"|..., ...}`. No hay ambigüedad porque "appendEvent" y las 4 acciones son conjuntos disjuntos. Se acepta la doble forma en vez de forzar una sola: el costo hoy es nulo y evita adivinar mal contra el §3, que nombra el RPC "appendEvent" pero también llama "Action" a la columna. Revisable en Fase 4 si conviene converger a una. | §3 usa "appendEvent" como nombre del RPC y "Action" como columna del Sheet, sin fijar la forma exacta del cuerpo POST. Aceptar ambas cierra la fase sin bloquear el frontend. | Bernardo / Claude Code | Locked |
| D-038 | 2026-08-13 | Apps Script expone solo `doPost`; no hay `doGet` ni endpoint de lectura propio. Toda lectura (People, fold de Events) va directo del frontend a Sheets API v4, tal como ya lo fija el diagrama de arquitectura del §3. | Pregunta abierta #4 de Fase 3: cómo se traduce el fold de Events al currentState de D-027. El spec ya lo resolvía; solo faltaba decirlo explícito antes de escribir código. | Bernardo / Claude Code | Locked |
| D-037 | 2026-08-13 | Harness de prueba para Fase 3: `tests/appsscript-smoke.test.js`, script Node plano (sin framework, misma convención de D-022) que pega directo al Web App desplegado — casos felices con write-then-verify real vía Sheets API v4, y cada rechazo server-side. Header-guard y concurrencia real quedan como procedimiento manual documentado en el propio archivo, no automatizados. | Pregunta abierta #3: cómo probar el backend sin frontend real todavía. | Bernardo / Claude Code | Locked |
| D-036 | 2026-08-13 | El fold de Events (D-009: último evento por (Task ID, Action) por Timestamp) se implementa en `dashboard/events.js` durante la Fase 3 — el módulo stub deja de ser stub — produciendo exactamente el shape `currentState` que D-027 fijó para liveMode. No se toca engine.js. | Pregunta abierta #4, mitad de "dónde vive el código", complementaria a D-038. | Bernardo / Claude Code | Locked |
| D-035 | 2026-08-13 | El código de Apps Script se commitea en el repo en `backend/Code.gs` (archivo único para v1). La URL del Web App desplegado NO se commitea en esta fase (no hay frontend todavía que la necesite); se lee desde variable de entorno en el test script. Se commiteará en `dashboard/config.js` en la Fase 4, cuando el Sprint Board la consuma. | No estaba fijado dónde vive el backend en el repo — necesario antes de escribir el .gs. | Bernardo / Claude Code | Locked |
| D-034 | 2026-08-13 | Event ID server-side con formato `E-<epochMillis>-<4 chars base36 random>`, generado dentro de doPost bajo LockService. Se prefiere sobre un contador monotónico porque no requiere leer/mantener estado adicional; LockService ya serializa la escritura. | Pregunta abierta #2 de Fase 3. | Bernardo / Claude Code | Locked |
| D-033 | 2026-08-13 | Esquema de columnas fijado: People = `Name \| Slack/Email \| Active` (Active boolean); Events = `Event ID \| Sprint ID \| Task ID \| Action \| Value \| Actor \| Timestamp \| Note` (Timestamp ISO completo, sin truncar — D-028 trunca solo al consumir en live mode); Settings = exactamente dos filas para v1, `onTrackBandWorkDays` y `opsWeekStartDay`, formato `Key \| Value \| Notes`. | Pregunta abierta #1 de Fase 3; el spec §3 daba los nombres pero no el detalle de implementación. | Bernardo / Claude Code | Locked |
| D-032 | 2026-08-13 | Una tarea in_progress sin statusChangedAt devuelve el error MISSING_STATUS_CHANGED_AT en vez de crashear — mismo tratamiento que MISSING_COMPLETED_AT para done. | D-027–D-031 solo definieron el caso done sin timestamp. El caso in_progress sin timestamp no debería ocurrir en la práctica (el propio evento setStatus=in_progress es lo que genera el timestamp), pero el manejo simétrico de errores evita un crash silencioso si algún día pasa. No ejercitado por ningún fixture. | Claude Code (implementación) / Bernardo (revisión) | Locked |
| D-031 | 2026-08-13 | El algoritmo de live mode (§4.7) corre en 3 pasadas sobre el mismo grafo/comparador de plan mode: (1) fijar tareas done a su fecha real de finalización; (2) fijar tareas in_progress a su inicio real, proyectando el fin con clamp a "hoy" si cae en el pasado; (3) agendar las open restantes con el loop exacto de §4.5/D-021, agregando today como tercer piso en startPos = max(depFinish, resourceFree, today). El rollup (§4.6) y el red-flag no cambian. | §4.7 solo dice la idea general ("fijar lo done, re-correr 4.5 desde hoy"); no define cómo conviven las tres categorías de status ni cómo se integra "hoy" al loop existente sin duplicar su lógica. | Bernardo / Claude Code | Locked |
| D-030 | 2026-08-13 | Seeding de availableFrom para una tarea done: el dueño se liberó del trabajo activo en completedAt − waitDays (calendario), no en completedAt. Asume que la espera real coincidió con la planeada. | El Events log (D-009) no timestampea por separado "trabajo terminado" vs. "ahora esperando"; solo hay un evento setStatus=done. Sin esta aproximación, una tarea con wait largo dejaría al dueño "ocupado" de más y correría en falso las siguientes tareas de esa persona. Marcada como aproximación práctica, no como certeza — revisar si algún sprint real la ejercita de forma contraintuitiva. | Bernardo / Claude Code | Locked |
| D-029 | 2026-08-13 | Una tarea in_progress fija su inicio en el statusChangedAt real (cuándo se marcó in_progress) y proyecta el fin con workDays/waitDays normales; si ese fin proyectado cae antes de "hoy", se clampea a hoy — una tarea que sigue abierta no puede haber terminado en el pasado. La tarea sale del loop de prioridad §4.4 (no compite por orden, ya está fija por la realidad, D-008). | §4.7 solo dice cómo tratar las done; no dice nada de in_progress. Sin el clamp, una tarea atrasada reportaría una fecha de fin ya pasada, lo cual no tiene sentido para algo que sigue sin terminar. | Bernardo / Claude Code | Locked |
| D-028 | 2026-08-13 | Los timestamps que consume live mode se truncan a su fecha calendario (YYYY-MM-DD); el motor trabaja a granularidad de día, igual que el eje de plan mode (D-020), que no tiene noción de hora del día. | El Events log guarda timestamp completo (D-009), pero el eje continuo del motor no tiene una unidad más fina que el día laboral; introducir hora-del-día complicaría D-020 sin que el dashboard (vista semanal) lo necesite. | Bernardo / Claude Code | Locked |
| D-027 | 2026-08-13 | Contrato de entrada de live mode, fijado antes de que exista la Fase 3: `liveMode(plan, currentState, todayISO)`, donde `currentState[taskId] = { status: 'open'\|'in_progress'\|'done', statusChangedAt: ISO \| null }` — exactamente la forma que produce el fold de Events por (taskId, "setStatus"), último por timestamp (D-009). `today` es un parámetro explícito, nunca `Date.now()` dentro del motor (testeable de forma determinista). | Sin fijar esto ahora, la Fase 3 (Sheet + Apps Script) podría terminar produciendo un shape distinto al que live mode espera, forzando un adaptador o un rediseño. | Bernardo / Claude Code | Locked |
| D-026 | 2026-08-13 | Una tarea degenerada con workDays:0 Y waitDays:0 (no existe en Rock 3) se clampea para terminar en su propio día de inicio, en vez de reportar el día anterior por la convención de posición-entera del eje continuo (D-020). | §7 contempla la tarea de pura espera (workDays:0, waitDays>0) pero no el caso doble-cero. Sin el clamp, el mapeo posición→fecha reportaría un finish un día antes del start. | Bernardo / Claude Code | Locked |
| D-025 | 2026-08-13 | Dos interpretaciones que Rock 3 NO ejercita, registradas como provisionales: (a) executionOrder (§4.4) es por-persona y solo ordena tareas con la misma firma de dueño — pares de dueños distintos caen a la prioridad §4.4; (b) owner:"Both" ocupa a TODAS las personas de `people` (en Rock 3, people == owners del rock == 2, así que es exacto). Confirmar ambas antes del primer uso real: uso de executionOrder (p.ej. la palanca de compresión de D-023), o un sprint con más participantes que los owners de un Rock (donde "Both" podría querer decir "los owners del Rock", no "todos"). | §4.4 no define la interacción de executionOrder con tareas "Both" ni entre personas; §4.3 dice "Both consume ambos owners" sin distinguir owners-del-Rock vs participantes-del-sprint. Con Rock 3 no hay diferencia observable; se registra la lectura actual sin bloquear, para revisarla con datos que la ejerciten. | Bernardo / Claude Code | Provisional — confirmar antes del primer uso |
| D-024 | 2026-08-13 | engine.js (OpsDashEngine) reutiliza OpsDashValidate para el índice de ids y el fan-out de dependencias del §4.2, en vez de duplicar esa lógica (ya probada en Fase 1); la resuelve desde window con fallback a globalThis (para Node). Implica orden de carga fijo en index.html (validate antes que engine), que ya es la convención de la casa. | §4 no dice de dónde sale la resolución de ids; duplicarla arriesga que dos copias se desincronicen (la cicatriz D-088 del testimonial-dashboard). Reusar la única fuente probada es más seguro. | Bernardo / Claude Code | Locked |
| D-023 | 2026-08-13 | En plan mode, Rock 3 cierra el 2026-09-14 (M4, M7, M20, M23 caen en goLive, un día pasado sprint.end 2026-09-13), así que el dashboard lo marcará en rojo desde que carga el plan. Se decide NO ajustar el motor ni la data para forzar el cierre en fecha: el motor dice la verdad. Si en el futuro se quiere comprimir, la palanca prevista es executionOrder (§4.4), no tocar el engine. | El objetivo del on-track es ser el indicador líder honesto que Asana no da; "arreglar" el rojo falseando duraciones u orden derrotaría el propósito. El overshoot es de 1 día y depende del desempate, no es catastrófico. | Bernardo | Locked |
| D-022 | 2026-08-13 | El harness de pruebas del motor vive en /tests: un script Node plano sin framework (tests/engine.test.js) que carga sprint-plan.json, corre el engine en plan mode y compara contra tests/expected-plan-mode.json (fixture congelado, 46 tareas activas, generado en esta sesión de diseño). El fixture es el árbitro de la semántica de fechas; si el engine y el fixture discrepan, se resuelve en el chat de diseño, nunca cambiando uno de los dos en silencio. | La Fase 1 dejó su harness sin commitear (fuera de scope entonces). El motor es la pieza de mayor riesgo; necesita red de regresión permanente. "Sin framework" mantiene la convención "no build step" del resto de los dashboards. | Bernardo | Locked |
| D-021 | 2026-08-13 | El algoritmo de scheduling es determinista y se fija explícitamente por encima de la redacción "por lotes" del §4.5: se recomputa el conjunto ready y se re-ordena globalmente por prioridad (§4.4: a=desbloquea más downstream, b=hardDeadline más temprano, c=mayor duración total) DESPUÉS de agendar cada tarea, agendando una tarea por iteración. Desempate final y total: task.id ascendente. | §4.5 está redactado como si agendara todas las ready de una pasada; con fracciones y readiness que cambia dentro del lote, dos implementaciones podrían diferir. Recomputar tras cada tarea + desempate por id garantiza que el resultado sea único y reproducible (condición para que el fixture de D-022 tenga sentido). | Bernardo | Locked |
| D-020 | 2026-08-13 | El motor usa un eje continuo de días laborales para las duraciones fraccionarias (cada día laboral = ancho 1.0; fines de semana = ancho 0), de modo que start + workDays es suma real sin lógica especial de findes, y dos tareas de medio día del mismo dueño pueden caer el mismo día calendario (cumpliendo §4.1). Semántica de fin: (a) tarea con waitDays=0 → su fin queda en posición continua (puede ser mitad de día) y un dependiente del mismo dueño puede seguir ese mismo día; (b) tarea con waitDays>0 → el fin se ancla al inicio del día laboral en/tras (fin-de-trabajo + waitDays días calendario); (c) availableFrom del dueño se libera en la posición de fin del trabajo activo, libre DURANTE la espera (§4.3/§4.5). | §4.1 dice que 0.5 workDay ocupa medio día laboral, pero §4.5 no explicita cómo conviven fracciones ni cómo se ancla el fin cuando hay espera calendario. Sin fijarlo, tareas fraccionarias encadenadas (p.ej. M6-t1→M6-t3, ambas Bernardo 0.25d) se calcularían mal. El eje continuo es la lectura correcta de §4.1 y simplifica la aritmética. | Bernardo | Locked |
| D-019 | 2026-08-13 | La validación (§7) distingue errores (bloquean: ok=false) de warnings (no bloquean). Errores: todo lo de §2/§7, más sprint.start/end no-ISO, end antes de start, y people que contenga "Both" (reservado para owner conjunto). Warnings: status presente en el JSON (§2 dice ignorarlo), schemaVersion desconocida, desc vacía, milestone sin tasks, y dependencia a un milestone vacío (se satisface vacuamente). Cada error/warning nombra el id y su ruta. | §7 solo pide "un error claro". La taxonomía permite cargar el plan mostrando todos los problemas juntos en vez de abortar al primero, y separa lo que frena la carga de lo que solo merece aviso. | Bernardo | Locked |
| D-018 | 2026-08-13 | La detección de ciclos corre sobre dos grafos por separado: (1) el grafo de scheduling de tasks de §4.2, y (2) el grafo declarativo de dependsOn entre milestones. Las aristas de milestone NO se agregan al grafo de scheduling, para no alterar la semántica del motor que §4.2 define estrictamente. | §4.2 solo define el grafo de tasks; los milestones también traen dependsOn (redundante con sus tasks en Rock 3). Revisarlos aparte detecta un ciclo declarativo sin contaminar el scheduling. Las tasks deferred se incluyen en la detección porque un ciclo latente reaparece al activarlas. | Bernardo / Claude Code | Locked |
| D-017 | 2026-08-13 | El deferred:true a nivel milestone se propaga a todas sus tasks: se excluyen del scheduling, del denominador de progreso (§5.1) y de la proyección, y se muestran en estado muted. En Rock 3 es redundante (M13 y su única task M13-t1 ya están ambas marcadas deferred), así que hoy no cambia ningún conteo; la regla existe para planes futuros donde solo se marque el milestone. | El schema de §2 solo define deferred en task, pero el fragmento ilustrativo de §2 y el seed marcan M13 (milestone) como deferred. Se fija la semántica antes del motor (Fase 2) porque afecta §4.6 (rollup) y §5.1 (denominador). | Bernardo | Locked |
| D-016 | 2026-08-13 | Se resuelve D-005: se mantienen ambos archivos (Opción B). sprint-plan.json en la raíz del repo es producción (copia del contenido de Rock 3, conforme al §10); data/rock3-seed.json se conserva como fixture de prueba para el motor de fechas. | D-005 quedó abierto en la sesión de scaffold; ahora que arranca la Fase 1 (carga + validación) había que fijar la ruta de producción antes de escribir el fetch. | Bernardo | Locked |
| D-015 | 2026-08-12 | v1 does not edit plan structure in-app. To change the plan (Rocks, milestones, tasks, durations, dependencies), regenerate `rock3-seed.json` and re-commit it. The dashboard reads structure, never writes it. | Keeps v1 simple and keeps a single source of truth for structure (the JSON). In-app structure editing is a large UI surface deferred out of v1. | Bernardo | Locked |
| D-014 | 2026-08-12 | Vista 4 (dashboard auto-reading task status from the Rock Projects) is deferred, not built in v1. Downstream, post-build: retire Asana, teach the Project Builder to emit the JSON, and make sprint planning always produce Rock-3-level detail. | The Project Builder confirmed Rock Projects store NO machine-readable per-task status in GitHub — only milestone-level prose and authoritative state in Asana. So the dashboard is the source of truth for task status in v1 (manual marking), which is universal and task-level. If Vista 4 is ever pursued, the clean bridge is Claude Code writing a status.json with the dashboard's task ids. | Bernardo | Locked |
| D-013 | 2026-08-12 | v1 includes a "This Week" view (three buckets per person: done this week / working on / not started, week boundary Friday-after-L10 → Thursday), a manual pin (drop-down; drag-and-drop is later polish), and a deliverable-link box per task. | The This Week view is the mid-week accountability screen and the backbone of the restructured L10. The pin lets a person override the projection (pull a future task forward / postpone one). The link box makes the dashboard the single home for every deliverable. Operations week (Fri→Thu) is deliberately distinct from CoachPulse's coaching week (Thu→Wed). | Bernardo | Locked |
| D-012 | 2026-08-12 | Every manual pin move is validated by the engine first: pulling a task forward is refused if any dependency (across people or across Rocks) is not done — the dashboard names the missing dependency and its owner; postponing a task shows the downstream cascade before confirming. | A task can look available but still depend on someone else's unfinished work. The drop-down lists only engine-validated available tasks; a blocked task never appears as pullable. Reuses the same dependency resolution the date engine already does. | Bernardo | Locked |
| D-011 | 2026-08-12 | Progress is a duration-weighted bar (weight = workDays only, not wait time). On-track is a burn-up: actual completed work vs. the plan's expected completed work for today, against a frozen baseline. The Rock is the primary unit for both metrics; the sprint-wide roll-up is a small secondary summary. | Weighting by task count would mislead (a 21-day task must count more than a 1-day one). Comparing against the frozen plan curve (not flat calendar time) avoids false "behind" alarms during waits like t-shirt shipping, because the planned curve is also flat there. | Bernardo | Locked |
| D-010 | 2026-08-12 | The task board displays tasks grouped by milestone with a checkbox each, in no imposed order. Marking is what people do; the dashboard stores and shows, it does not direct the work. Task status enum: open / in_progress / done. | The Rock Project guides the work; the dashboard records state. The engine's priority ordering is used only internally to project dates, and is never shown on screen. | Bernardo | Locked |
| D-009 | 2026-08-12 | Identity is a person-selector (no auth), reusing the testimonial-dashboard pattern plus three improvements: single People source of truth (a People tab read by frontend and Apps Script), server-generated stable row id, and write-then-verify. Structure lives in the seed JSON; state lives in a single append-only Events log in a Google Sheet, keyed by task id. | Multi-user marking (everyone marks their own tasks, Bernardo verifies mid-week and at L10) requires persistence and identity, ruling out a static HTML. The testimonial pattern is proven (GitHub Pages + Sheet + Apps Script). Events log holds status, deliverable link, and pin as actions. | Bernardo | Locked |
| D-008 | 2026-08-12 | Dates are computed outputs, never typed. The engine schedules from workDays + waitDays + dependencies + per-person capacity (one thing at a time; "Both" occupies both queues). When a person has several ready tasks, the engine auto-orders by priority (unblocks others > hard deadline > longest) for projection ONLY; executionOrder in the JSON is an optional override. Real completion timestamps always reproject over the guess. | This is the reverse-engineering-from-durations model. The invisible ordering never appears on screen; reality overrides both the pin and the priority guess. | Bernardo | Locked |
| D-007 | 2026-08-12 | Data model is Sprint → Rock → Project → Milestone → Task. Task is the atomic unit: owner (a person or "Both"), type (work / meeting / approval), workDays, waitDays (calendar wait during which the owner is free), dependsOn and crossDependsOn (may point to any task or milestone in the sprint). Fractional durations allowed. Deferred tasks excluded from projection and the progress denominator. | Captures parallelism, wait time, cross-Rock/cross-person dependencies, and meeting/approval task types — validated against the real Rock 3 (6 projects, M1–M23, 47 tasks, 40.75 work-days). | Bernardo | Locked |
| D-006 | 2026-08-12 | Project runs on the Chat + Code + GitHub pattern in ONE repo (`F4LA/OperationsDashboard`): code + `/docs` memory (spec, decision log, project brain) + `/data` seed, mirroring the testimonial-dashboard layout. A "build" Code session writes code; a dedicated "push" Code session writes the `/docs` memory. A dedicated Claude Project thinks/specs/decides/reviews and pulls `/docs` at chat start. | Chosen over a separate memory repo after seeing that the testimonial-dashboard keeps memory and code together in one repo and it works. Two Code sessions on the same repo don't collide because each edits different files and always pulls before pushing. | Bernardo | Locked |
| D-005 | 2026-08-12 | Seed data committed this session at `data/rock3-seed.json`, not at the repo root as `sprint-plan.json`. | Spec §10 states the production raw-URL path is `.../main/sprint-plan.json` at repo root. This session is scaffold-only (no fetch/engine logic yet), so the seed was placed where the setup instructions asked. **Revisit when §9 step 1's load/validate logic is built** — decide then whether to move/rename to match §10, or keep both a root `sprint-plan.json` (production) and `data/rock3-seed.json` (test fixture). | Bernardo (via setup instruction) | Superseded by D-016 |
| D-004 | 2026-08-12 | Stub modules scaffolded (empty, no logic): `dashboard/config.js`, `dashboard/validate.js`, `dashboard/engine.js`, `dashboard/events.js`, `dashboard/metrics.js`, `dashboard/board.js`, `dashboard/network.js`, `dashboard/thisweek.js`, plus root `app.js` and `styles.css`. | One stub per spec piece (§2–§6); `styles.css` added to match CoachPulse's file layout even though not explicitly requested. | Bernardo / Claude Code | Locked |
| D-003 | 2026-08-12 | Window-global prefix for every module is `OpsDash` (e.g. `OpsDashConfig`, `OpsDashEngine`, `OpsDashBoard`). | Mirrors CoachPulse's own `root.CoachPulse<Module>` attach pattern; kept short per that precedent. | Bernardo | Locked |
| D-002 | 2026-08-12 | Module stubs live in `/dashboard`, with `app.js` as the top-level bootstrap/orchestrator at repo root (not `/js`). | CoachPulse (the reference dashboard, inspected on disk) uses `/dashboard` + root `app.js`. Matching the real convention over the informal `/js` suggestion. | Bernardo | Locked |
| D-001 | 2026-08-12 | Repo created as `F4LA/OperationsDashboard`, public, PascalCase. | Matches the naming convention of the other Strong Standard dashboards and spec §10's stated repo name. | Bernardo | Locked |
